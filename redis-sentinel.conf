# Redis Sentinel configuration template
# ${REDIS_PASSWORD} is substituted at container startup by docker/sentinel-entrypoint.sh
# Do NOT use this file directly — it is rendered at runtime by the entrypoint script.

sentinel monitor mymaster redis-master 6379 2
sentinel auth-pass mymaster ${REDIS_PASSWORD}
sentinel down-after-milliseconds mymaster 5000
sentinel failover-timeout mymaster 10000
sentinel parallel-syncs mymaster 1
