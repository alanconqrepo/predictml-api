metrics:
  predictions_prod: Production predictions returned to clients (is_shadow=False) over
    the period. Does not include shadow executions.
  shadow_predictions: 'Silent predictions computed in the background (is_shadow=True),
    not returned to the client.

    Used to compare new versions against production without affecting real traffic.'
  modeles_actifs: 'Number of distinct models with at least one active version (is_active=True)
    with predictions in the selected period.

    A model is "active" as soon as a version is loadable, regardless of its deployment
    mode (Production, A/B, Shadow, or no routing).

    Not to be confused with "in production": an active model may only be in shadow
    or uploaded without being exposed to traffic.'
  alertes_sante: "Number of models in alert over the period.\nEach model's health\
    \ status is the worst of 4 indicators:\n• Execution error rate\n  \U0001F7E1 ≥\
    \ 5%  ·  \U0001F534 ≥ 10%\n\n• Feature drift (Z-score + PSI + null rate)\n  Z-score\
    \ \U0001F7E1 ≥ 2  · \U0001F534 ≥ 3\n  PSI \U0001F7E1 ≥ 0.1  ·  \U0001F534 ≥ 0.2\n\
    \  Null rate \U0001F7E1 gap ≥ 5 pts  ·  \U0001F534 ≥ 15 pts or > 30%\n\n• Performance\
    \ drift (accuracy 1st vs 2nd half of period)\n  \U0001F7E1 drop ≥ 5 pts  ·  \U0001F534\
    \ drop ≥ 10 pts\n\n• Output drift (PSI of prediction distribution)\n  \U0001F7E1\
    \ PSI ≥ 0.1  ·  \U0001F534 PSI ≥ 0.2\n\nThe final status is the most severe of\
    \ these four. ⚪ no_data / no_baseline does not degrade the global status."
  taux_erreur: 'Proportion of requests that failed execution over the period.

    ⚠️ This is NOT an ML quality indicator: a prediction may be correctly computed
    and returned to the client, yet wrong on a business level.

    An execution error means the API could not produce a prediction (server exception,
    model not loaded, timeout, invalid input…). Computed on production predictions
    only (excluding shadow).

    🟡 Warning: ≥ 5%  ·  🔴 Critical: ≥ 10%'
  accuracy: Proportion of correct predictions. 1.0 = 100% correct predictions.
  auc: 'AUC-ROC (Area Under the ROC Curve): area under the Receiver Operating Characteristic
    curve.

    Measures the model''s ability to distinguish classes, independently of the decision
    threshold.

    • 1.0 = perfect discriminant • 0.5 = no better than chance (random classifier)
    • < 0.5 = worse than chance

    Interpretation: • 🟢 ≥ 0.90 → excellent • 🟡 0.70–0.90 → good to acceptable • 🔴
    < 0.70 → needs improvement

    Requires predicted probabilities (model must output confidence scores, not just
    a class). Binary classification: standard AUC. Multiclass: OvR (One vs Rest) weighted.'
  mae: 'Mean Absolute Error: average absolute deviation between prediction and actual
    value. Lower is better.'
  rmse: 'Root Mean Square Error: square root of the mean squared error. Penalises
    large errors more than MAE.'
  r2: 'Coefficient of determination: share of variance explained by the model. 1.0
    = perfect model, 0 = null model, negative = worse than average.'
  precision: Among positive predictions, proportion that are truly positive. Evaluates
    the reliability of positive detections. (w.) = weighted average per class.
  recall: Among real positive cases, proportion correctly detected. Evaluates the
    ability not to miss positive cases. (w.) = weighted average per class.
  f1: Harmonic mean of precision and recall. Useful when classes are imbalanced. (w.)
    = weighted average per class.
  latence_mediane: 'Median response time: half of all requests have a time below this
    value.'
  latence_avg: Average response time across all requests.
  latence_p95: '95th percentile latency: 95% of requests are processed in less than
    this time. Key production performance indicator.'
  concordance_shadow: 'Agreement rate between the shadow model and the production
    model: proportion of cases where both models give the same prediction.'
  p_value: Probability of observing a difference this large if the two versions were
    equivalent. Below the threshold (e.g. 0.05) → statistically significant difference.
  niveau_confiance: 'Confidence level of the test. 95% means: we accept a 5% risk
    of wrongly concluding there is a difference when there is none.'
  test_statistique: Statistical test used to compare versions. Chi-² for error rates
    (proportions), Mann-Whitney U for latency distributions (continuous).
  metrique_analysee: 'Metric on which the statistical test is performed: error rate
    or response time.'
  brier_score: 'Brier score: measures the accuracy of predicted probabilities. 0 =
    perfect (exact probabilities), 1 = worst case. Below 0.25 is generally acceptable.'
  gap_confiance: Gap between average predicted confidence and actual observed accuracy.
    Positive = overconfidence (model overestimates its certainty), negative = underconfidence.
    Ideally close to 0.
  statut_calibration: 'Model calibration status: OK if the gap is small, Overconfident
    if the model overestimates its probabilities, Underconfident if it underestimates
    them.'
  calib_mae: 'MAE (Mean Absolute Error): average absolute error between predicted
    ŷ and actual y.

    MAE = mean(|ŷ − y|)

    Lower is better. Same unit as the target variable.'
  calib_rmse: 'RMSE (Root Mean Square Error): square root of the mean squared error.

    RMSE = √(mean((ŷ − y)²))

    Penalises large errors more than MAE. Same unit as the target variable. Lower
    is better.'
  calib_r2: 'R² (coefficient of determination): share of variance explained by the
    model.

    R² = 1 − SS_res / SS_tot SS_res = Σ(ŷ − y)²,  SS_tot = Σ(y − ȳ)²

    1.0 = perfect model · 0 = no better than average · negative = worse than average.
    Ideally > 0.80.'
  calib_biais: 'Systematic model bias: mean of residuals (ŷ − y).

    Bias = mean(ŷ − y)

    Positive → model over-estimates on average. Negative → model under-estimates on
    average. Close to 0 → no systematic bias (good calibration).

    Status: 🟢 OK if |relative bias| < 10% of the std dev of observed y, 🟡 Over-estimation
    or Under-estimation otherwise.'
  confiance_moyenne: Average maximum probability returned by the model for its predictions.
    Indicates how 'confident' the model is on average.
  p25_confiance: '25th confidence percentile: 25% of predictions have a confidence
    below this value.'
  p75_confiance: '75th confidence percentile: 75% of predictions have a confidence
    below this value.'
  tendance_performance: Performance comparison between the 1st and 2nd half of the
    selected period. A drop indicates potential model drift.
  predictions_shadow: Number of predictions executed in shadow mode (is_shadow=True),
    not returned to the client. Used to compare new versions in parallel.
stats:
  page_title: Stats — PredictML
  title: 📈 Statistics
  caption: "Statistics based on **production predictions** (is_shadow=False). Volume, success rate, P95 latency and model performance leaderboard."
  btn_refresh: 🔄 Refresh
  date_start: Start date
  date_end: End date
  error_date_range: End date must be after start date.
  no_models: No models available.
  drift:
    ok: 🟢 ok
    warning: 🟡 warning
    critical: 🔴 critical
    no_baseline: ⚪ no baseline
    no_data: ⚪ no data
    insufficient_data: ⚪ insufficient data
    unknown: ⚪ unknown
  leaderboard:
    expander: 🏆 Leaderboard — Production models
    sort_by: Sort by
    sort_options:
      accuracy: Accuracy
      auc: AUC-ROC
      f1_score: F1 Score
      mae: MAE (regression)
      r2: R² (regression)
      rmse: RMSE (regression)
      latency_p95_ms: p95 latency
      predictions_count: Prediction volume
    tab_table: Table
    tab_scatter: Comparison
    no_models_prod: No production models found.
    col_rank: Rank
    col_model: Model
    col_version: Version
    col_accuracy: Accuracy
    col_auc: AUC-ROC
    col_f1: F1 Score
    col_mae: MAE
    col_r2: R²
    col_rmse: RMSE
    col_latency: p95 Latency (ms)
    col_drift: Drift
    col_predictions: Model predictions ({days}d)
    col_version_predictions: "Version predictions ({days}d)"
    col_version_predictions_help: "Number of predictions made by this specific version over the last {days} days. Compare with « Model predictions » which totals all versions combined."
    col_date_start: Version start
    col_date_start_help: Date of the first prediction recorded with this version in production (all time).
    col_date_end: Version end
    col_date_end_help: Date of the last prediction recorded with this version in production (all time).
    col_accuracy_help: Model accuracy in production. Computed from predictions in
      the selected period with available observed results (classification). Between
      0 and 1 — closer to 1 is better.
    col_auc_help: Area under the ROC curve — measures the model's discriminatory ability
      independent of threshold (classification). 1.0 = perfect, 0.5 = random. Requires
      predicted probabilities.
    col_f1_help: Model F1-score in production. Computed from predictions in the
      selected period with available observed results. Robust to imbalanced classes.
      Between 0 and 1.
    col_mae_help: Mean Absolute Error (regression). Average absolute deviation between
      prediction and actual value, expressed in the unit of the target variable — lower
      is better. Sorted ascending when selected. '—' for classification models.
    col_r2_help: 'R² coefficient of determination (regression). Measures the share
      of variance explained by the model: 1.0 = perfect, 0.0 = null model, < 0 = worse
      than average. ''—'' for classification models.'
    col_rmse_help: Root Mean Square Error (regression). Average error in the unit
      of the target variable — lower is better. Sorted ascending when selected. '—'
      for classification models.
    col_latency_help: 95th percentile response time over the selected period. 95%
      of requests were processed in less than this time.
    col_drift_help: Deviation between production feature distribution and training
      baseline. 🔴 critical = significant drift, model sees data different from training
      data.
    col_predictions_help: Total predictions over the last {days} days (includes shadow
      predictions is_shadow=True).
    col_task: Task
    col_task_help: "Task type detected at upload: Binary (2 classes), Multiclass (≥
      3 classes), or Regression."
    col_task_binary: 🔵 Binary
    col_task_regression: 📈 Regression
    col_task_multiclass: 🟡 Multiclass
    col_task_unknown: "—"
    col_status: Status
    col_status_help: "Deployment mode of the version: A/B test, Shadow, Active or Production."
    filter_model: Filter by model
    filter_version: Filter by version
    filter_all_models: All models
    filter_all_versions: All versions
    kpis_hidden_no_pred: Performance KPIs hidden (no predictions in the selected period).
    scatter_x_axis: X Axis
    scatter_label_latency: p95 Latency (ms)
    scatter_label_accuracy: Accuracy
    scatter_label_auc: AUC-ROC
    scatter_label_f1: F1 Score
    scatter_label_mae: MAE
    scatter_label_r2: R²
    scatter_label_rmse: RMSE
    scatter_label_volume: Prediction volume
    scatter_threshold_max: Threshold {label} (max)
    scatter_threshold_min: Threshold {label} (min)
    scatter_threshold_x_help: Displays a vertical line on the X axis ({label}). 0
      = disabled.
    scatter_threshold_y_help: Displays a horizontal line on the Y axis ({label}).
      0 = disabled.
    scatter_filtered: '{n} model(s) without {x_label} or {y_label} excluded from chart.'
    scatter_no_data: 'Insufficient data: no model has both {x_label} and {y_label}
      values.'
    scatter_hover_predictions: 'Predictions: {count}'
    scatter_hover_drift: 'Drift: {label}'
    scatter_vline_max: Max {label} {value}
    scatter_vline_min: Min {label} {value}
    scatter_vline_threshold: Threshold {label} {value}
    scatter_hline_max: Max {label} {value}
    scatter_hline_min: Min {label} {value}
    scatter_hline_threshold: Threshold {label} {value}
  aggregated:
    expander: 📊 Aggregated statistics by model
    filter_name: Filter by name
    filter_placeholder: Search…
    model_select: Model
    model_all: (all)
    loading: Loading statistics...
    col_model: Model
    col_total: Total
    col_errors: Errors
    col_error_rate: Error rate
    col_avg_rt: Avg RT (ms)
    col_p50_rt: p50 RT (ms)
    col_p95_rt: p95 RT (ms)
    col_model_help: Name of the ML model for which statistics are computed.
    col_total_help: Total predictions served by this model over the selected period.
    col_errors_help: Number of predictions that returned an error (status 'error')
      over the period.
    col_error_rate_help: Percentage of predictions in error. A rate > 5% warrants
      attention, > 10% is critical.
    col_avg_rt_help: Average response time in milliseconds. Includes model loading
      and computation.
    col_p50_rt_help: 'Median response time: 50% of requests are processed in less
      than this time.'
    col_p95_rt_help: '95th percentile response time: 95% of requests are processed
      in less than this time. Key SLA indicator.'
    no_data: No data for this period.
    load_error: 'Could not load aggregated statistics: {error}'
    no_predictions: No predictions in the selected period.
    kpi_total: Total predictions
    kpi_error_rate: Error rate
    kpi_median_rt: Median response time
    kpi_models_used: Models used
    chart_by_model: Distribution by model
    chart_rt_dist: Response time distribution
    chart_rt_no_data: Response time data not available.
    chart_label_model: Model
    chart_label_nb_pred: Predictions
    chart_label_time_ms: Time (ms)
    chart_label_density: Density
    chart_daily: Predictions per day
    chart_label_date: Date
    chart_errors_daily: Errors per day — by model
    chart_no_errors: ✅ No errors in the selected period.
    chart_label_nb_errors: Errors
    chart_boxplot: Response time by model (box plot)
  accuracy_timeline:
    expander: 📈 Accuracy over time — multi-model comparison
    models_label: Models to compare
    granularity: Granularity
    gran_day: Day
    gran_week: Week
    no_model_selected: Select at least one model.
    loading: Loading performance metrics…
    unavailable: '⚠️ Data unavailable for: {models}'
    no_data: No accuracy data for the selected period. Submit results via **POST /observed-results**
      to activate tracking.
    hover_pairs: 'Obs. pairs: %{{customdata[0]}}'
    col_model: Model
    col_observed_pairs: Observed pairs
    col_observed_pairs_help: Number of predictions for which an observed result has
      been submitted.
    col_metric_help: Last known value over the period. Accuracy = proportion of correct
      predictions (classification). MAE = mean absolute error (regression).
    metric_label_accuracy: Accuracy
    metric_label_mae: MAE
    metric_label_mixed: Accuracy / MAE
    legend_mae_suffix: (MAE)
  multi_metric:
    expander: 📊 Multi-metric evolution — one model
    model_label: Model
    version_label: Version
    version_all: (all)
    granularity: Granularity
    gran_day: Day
    gran_week: Week
    gran_month: Month
    metrics_label: Metrics to display
    metric_accuracy: Accuracy
    metric_auc: AUC-ROC
    metric_f1: Weighted F1
    metric_pairs: Observed pairs
    metric_mae: MAE
    metric_rmse: RMSE
    no_metric_selected: Select at least one metric.
    no_data: No observed-results data for this model in the selected period. Submit
      results via **POST /observed-results** to activate tracking.
    load_error: 'Could not load metrics: {error}'
    bar_pairs: Observed pairs
    axis_date: Date
    axis_pairs: Observed pairs
    summary_caption: 'Aggregated values over the entire selected period:'
  drift_performance:
    expander: 📉 Performance drift — rolling metric
    filter_name: Filter by name
    filter_placeholder: Search…
    model_label: Model (drift)
    metric_label: Metric
    alert_checkbox: Enable threshold alert
    slider_min: Threshold {label} (min)
    input_max: Threshold {label} (max)
    slider_help_min: Alert if the rolling average falls below this threshold.
    input_help_max: Alert if the rolling average exceeds this threshold.
    date_start: Start date
    date_end: End date
    date_error: ⚠️ Start date must be before end date.
    no_data: Not enough observed data for this model in the selected period ({date_start}
      → {date_end}). Submit results via POST /observed-results to activate tracking.
    load_error: 'Could not load performance metrics: {error}'
    wrong_type: 'Metric **{metric}** is not available for this model (detected type:
      **{model_type}**). It applies to **{expected}** models.'
    expected_classif: classification
    expected_regress: regression
    rolling_short_label: '{n}d rolling avg'
    rolling_long_label: '{n}d rolling avg'
    kpi_last_day: '{metric} (last day)'
    kpi_matched: Predictions with observed result ({days}d)
    alert_breach: '⚠️ Drift detected: the {n}d rolling average of **{metric}** ({current})
      is {direction} the configured threshold ({threshold}).'
    direction_below: below
    direction_above: above
    daily_label: Daily {metric}
    seuil_min: Min threshold {value}
    seuil_max: Max threshold {value}
ab_testing:
  page_title: A/B Testing — PredictML
  title: 🧪 A/B Testing & Shadow Deployment
  btn_refresh: 🔄 Refresh
  caption: Configure traffic splits between versions of the same model and compare
    their metrics in real time.
  load_error: 'Could not load models: {error}'
  no_models: No models available.
  filter_name: Filter by name
  filter_placeholder: Search for a model…
  model_select: 🔍 Model
  config:
    expander: ⚙️ A/B / Shadow Configuration
    description: Configure the deployment mode and traffic weight for each version.
      The sum of A/B weights must be **≤ 1.0**.
    col_version: '**Version**'
    col_mode_current: '**Current mode**'
    col_weight_current: '**Current weight**'
    col_mode_new: '**New mode**'
    col_weight_new: '**New weight**'
    mode_no_change: —
    weight_no_change: —
    weight_sum: '**Sum of A/B weights:** {badge} `{value}` / 1.0'
    weight_overflow: ⚠️ The sum of weights exceeds 1.0 — fix before applying.
    apply_btn: ✅ Apply configuration
    updated: '{count} version(s) updated.'
    admin_only: 🔒 A/B configuration is reserved for administrators.
  comparison:
    subheader: 📊 Version comparison
    date_start: Start date
    date_end: End date
    no_predictions: No predictions recorded for this model in the selected period.
    load_error: 'Could not load comparison data: {error}'
    col_version: Version
    col_mode: Mode
    col_weight: Weight
    col_weight_help: Traffic share allocated to this version.
    col_algorithm: Algorithm
    col_created: Created on
    col_creator: Creator
    col_pred_prod: Pred. (prod)
    col_shadow: Shadow
    col_output: Predicted output
    col_output_help: 'Regression: weighted mean of predicted values (µ). Classification:
      distribution of predicted classes over the period (top 3).'
    col_err_pct: Err. (%)
    col_lat_avg: Lat. avg (ms)
    col_lat_p95: Lat. p95 (ms)
    col_concordance: Concordance (%)
    col_accuracy: Accuracy
    col_accuracy_help: Accuracy on the training test set.
    col_f1: F1
    col_f1_help: F1-score on the training test set.
    col_r2: R²
    col_r2_help: Coefficient of determination (regression).
    col_rmse: RMSE
    col_rmse_help: Root Mean Squared Error (regression).
  ab_test:
    expander: 🟠 A/B Test
    sig_title: 🔬 Statistical significance
    metric_select_label: Test metric
    metric_auto: Auto (smart selection)
    metric_error_rate: Error rate — Chi-²
    metric_mae: Prediction MAE — Mann-Whitney
    metric_auc: AUC — Hanley-McNeil Z-test
    metric_response_time: Response latency — Mann-Whitney
    no_data_reason_error_rate: Chi-² requires observed results (ground truth) and at
      least one misclassification in one of the groups
    no_data_reason_mae: Mann-Whitney on MAE requires prediction residuals (regression
      model with observed-results)
    no_data_reason_auc: Hanley-McNeil test requires probability scores and observed
      results for a binary model (at least 10 pairs with both classes)
    no_data_reason_response_time: Mann-Whitney on latency requires at least 2 predictions
      per version
    no_data_metric: ⚠️ Not enough data for metric **{metric}** in this period ({reason}).
    no_data_auto: 💡 The significance test will be available once two versions have
      accumulated predictions in the selected period.
    metric_fallback_warning: '⚠️ Metric **{requested}** was not available — test performed
      on: **{actual}**.'
    significant_with_winner: ✅ **Statistically significant difference** — version
      **{winner}** is better on metric *{metric}*
    significant_no_winner: ✅ **Statistically significant difference** between the
      two versions.
    not_significant: ⚠️ **Non-significant difference** — cannot conclude with current
      data (p = {p_value}, threshold = {threshold})
    winner_banner: '🟢 **Winner identified: `{winner}`** — p-value = **{p_value}**
      · confidence {confidence}'
    promote_btn: 🏆 Promote to production
    promote_success: ✅ `{winner}` promoted to production successfully.
    kpi_test: Statistical test
    kpi_metric: Metric analysed
    kpi_pvalue: p-value
    kpi_pvalue_delta: threshold {threshold}
    kpi_confidence: Confidence level
    gauge_pvalue_title: p-value (threshold α = {threshold})
    gauge_power_title: Statistical power (current data vs required)
    samples_title: '**Available observations per version**'
    col_samples_version: Version
    col_samples_version_help: Version number of the model participating in the A/B
      test.
    col_samples_current: Current observations
    col_samples_current_help: Number of predictions recorded for this version in the
      selected period.
    col_samples_min: Recommended minimum
    col_samples_min_help: Minimum number of observations needed for the test to be
      statistically reliable (80% power).
    col_samples_ok: Sufficient?
    col_samples_ok_help: '✅ Yes: enough data to conclude. ❌ No: keep accumulating
      predictions.'
    samples_enough: ✅ Yes
    samples_not_enough: ❌ No ({missing} missing)
    interp_expander: 💡 How to interpret this result?
    interp_body: '**Test used:** {test_label} - **Chi-²**: compares error rates between
      two versions via a success/error contingency table. - **Mann-Whitney U**: compares
      latency distributions (used as fallback if no errors are observed).

      **p-value = {p_value}** - If `p < {threshold}` → the observed difference has
      less than {risk} chance of being due to chance. - If `p ≥ {threshold}` → not
      enough data or no real difference.

      **Recommended minimum: {min_needed} observations/version** (for 80% statistical
      power) - Computed via Cohen''s h effect size (error rate) or Cohen''s d (latency).'
    interp_conclusion_promote: '✅ **Conclusion: you can safely promote** `{winner}`
      **to production.**'
    interp_conclusion_wait: '⚠️ **Conclusion: keep accumulating data** before making
      a promotion decision.'
    traffic_title: '**Traffic distribution (production predictions)**'
    traffic_col_version: Version
    traffic_col_pred: Predictions
    traffic_no_prod: No production predictions recorded.
    dist_regression_title: '**Distribution of predicted values by version (production)**'
    dist_classification_title: '**Distribution of labels by version (production)**'
    dist_predicted_value: Predicted value
    dist_label: Label
    dist_count: Count
    dist_no_data: No distribution available.
  shadow:
    expander: 🟣 Shadow Deployment
    concordance_title: '**🔮 Shadow vs production concordance**'
    concordance_caption: Fraction of shadow predictions that match the production
      prediction for the same `id_obs`. Requires `id_obs` to be set in requests.
    concordance_col_version: Shadow version
    concordance_col_rate: Concordance
    concordance_col_score: Score
    concordance_no_data: 💡 The shadow/production concordance rate will be computed
      automatically once predictions with `id_obs` are recorded for both versions.
    concordance_title_regression: '**🔮 Shadow vs production prediction distribution**'
    concordance_caption_regression: Compared distribution of predicted values by the
      shadow version and the production version, computed on recent predictions.
    concordance_no_data_regression: No distribution data available to compare shadow
      and production versions.
    regression_dist_col_role: Version
    regression_dist_col_pred: Predicted value
    regression_role_shadow: Shadow
    regression_role_prod: Production
    regression_dist_comparison_title: Prediction distribution — shadow vs production
    kpi_shadow_mean: Shadow mean
    kpi_shadow_mean_help: Mean of predicted values by the shadow version (computed from
      the recent prediction distribution).
    kpi_prod_mean: Production mean
    kpi_prod_mean_help: Mean of predicted values by the production version (computed
      from the recent prediction distribution).
    kpi_pred_bias: Shadow bias
    kpi_pred_bias_help: "Shadow mean − Production mean. Positive\xA0= shadow predicts
      higher values on average."
    enriched_title: 🔮 Enriched shadow analysis
    enriched_caption: Differential metrics between the shadow version and the production
      version on predictions with a common `id_obs`.
    load_error: Could not load shadow data.
    no_shadow_version: No version in shadow mode for this model.
    vs_banner: '**Shadow `{sv}`** vs **Production `{pv}`** — {badge} *{recommendation}*'
    rec_shadow_better: Shadow appears better
    rec_prod_better: Production remains better
    rec_equivalent: Equivalent performance
    rec_insufficient: Insufficient data to conclude
    kpi_pairs: Comparable pairs
    kpi_agreement: Prediction agreement
    kpi_agreement_help: Fraction of id_obs where shadow and production predict the
      same value.
    kpi_conf_delta: Δ Confidence
    kpi_conf_delta_help: max_confidence shadow − max_confidence production (positive
      = shadow more confident).
    kpi_lat_delta: Δ Latency (ms)
    kpi_lat_delta_help: Mean shadow latency − mean production latency (negative =
      shadow faster).
    kpi_accuracy: Shadow vs prod accuracy
    kpi_accuracy_help: Accuracy computed on pairs with an observed_result.
    kpi_accuracy_na: Accuracy
    kpi_accuracy_na_help: 'Not available: no observed_result for the compared id_obs.'
    gauge_title: Shadow / production agreement (%)
    promote_banner: 🟢 **Shadow `{sv}` appears better than production `{pv}`** — promote
      it to become the active version?
    promote_btn: 🚀 Promote shadow
    promote_success: ✅ `{sv}` promoted to production successfully.
    recent_title: 📋 Recent shadow predictions
    recent_count: '{count} shadow prediction(s)'
    recent_no_data: No shadow predictions recorded in this period.
    recent_load_error: 'Could not load predictions: {error}'
    col_id: ID
    col_id_help: Unique identifier of the prediction in the database.
    col_timestamp: Timestamp
    col_timestamp_help: Date and time at which the shadow prediction was made.
    col_version: Version
    col_version_help: Version of the model that produced this shadow prediction.
    col_id_obs: id_obs
    col_id_obs_help: Observation identifier, provided by the calling client. Used
      to match a shadow prediction to its production counterpart.
    col_result: Result
    col_result_help: Value predicted by the shadow version. Not returned to the client
      — used only for internal comparison.
    col_latency: Latency (ms)
    col_latency_help: Computation time of the shadow prediction in milliseconds.
    col_status: Status
    col_status_help: ✅ Success or ❌ Error during prediction computation.
    col_result_prod: Prod result
    col_result_prod_help: Value predicted by the production version for the same id_obs.
      Shows "—" if no matching production prediction is found for this id_obs.
login:
  title: 🤖 PredictML Admin Dashboard
  subtitle: Log in with your API token to access the dashboard.
  api_url_label: API URL
  token_label: API Token
  token_placeholder: Your Bearer token
  submit_btn: Log in
  error_no_token: Please enter a token.
  error_invalid_url: Invalid URL. It must start with http:// or https://
  error_invalid_token: Invalid token or API unreachable.
  no_token_expander: Don't have a token yet?
  no_token_body: '**First admin access**


    The admin token is defined by the **`ADMIN_TOKEN`** environment variable.


    ---


    **New user**


    Submit a request via the **"Access Request"** page in the menu — an admin will

    share your token once approved.

    '
  no_token_link: 📝 [Submit an access request](/Demande_Acces)
home:
  title: 🤖 PredictML Admin Dashboard
  role_admin: 👑 Admin
  role_user: 👤 User
  caption: 'Logged in — {role}  |  API: `{api_url}`'
  status:
    subheader: 🖥️ System status
    subheader_caption: Real-time state of the API, database, and models loaded in memory.
    api_label: API Status
    api_label_help: Health state of the FastAPI service. ✅ Online = all routes respond normally. ⚠️ Degraded = API responds but some internal components have errors.
    api_online: ✅ Online
    api_degraded: ⚠️ Degraded
    api_unknown: ❓ Unknown
    db_label: Database
    db_label_help: PostgreSQL connection status. ✅ Connected = database responds and queries work. ⚠️ = database is unreachable or in error.
    db_connected: ✅ Connected
    cache_label: Cached
    cache_label_help: Number of models currently loaded in memory (RAM). A cached model responds without cold-start latency — it does not need to be reloaded from MinIO on each request.
    cache_value: '{count} version(s)'
    alerts_label: Models in alert
    alerts_label_help: Number of models whose health status is "warning" or "critical" over the last 30 days. Check the Supervision page for details.
    alerts_count: 🔴 {count}
    alerts_critical_only: '🔴 {count} critical'
    alerts_warning_only: '⚠️ {count} warning(s)'
    alerts_both: '🔴 {crit} crit. · ⚠️ {warn} warn.'
    alerts_none: ✅ None
  kpis:
    subheader: 📊 Activity — last 30 days
    subheader_caption: Aggregated metrics across all prediction requests over the last 30 days.
    predictions: 📈 Predictions
    predictions_help: Total number of /predict requests received over the last 30 days, across all models and users.
    success_rate: ✅ Success rate
    success_rate_help: "Proportion of requests processed without a technical error: (total − errors) / total. An error = exception raised by the API (model not loaded, missing feature, timeout…). ⚠️ This does not measure prediction quality (accuracy vs ground truth) — see Supervision or Observed Results for that."
    latency_p95: ⚡ P95 Latency
    latency_p95_help: 95th percentile of API response time in milliseconds. 95% of requests responded in less than this duration. Average computed across all active models.
    coverage: 🔖 Ground truth coverage
    coverage_help: Percentage of predictions with an observed result (ground truth)
  alerts:
    subheader: 🚨 Active alerts
    subheader_caption: Models whose error rate, latency, or drift exceeds configured thresholds. Act quickly to avoid production impact.
    critical: '🔴 {count} model(s) in critical state: {names}'
    warning: '⚠️ {count} model(s) in alert: {names}'
    supervision_link: → View full supervision
  leaderboard:
    subheader: 🏆 Model leaderboard
    subheader_caption: Top 5 production versions ranked by accuracy over the last 30 days.
    col_model: Model
    col_version: Version
    col_mode: Mode
    col_accuracy: Accuracy
    col_predictions: Predictions
    col_drift: Drift
    full_link: 📊 Full leaderboard →
    active_models: Active models
    prod_versions: Production versions
    no_models: No models registered. Start by uploading a model.
    upload_link: 🤖 Upload a model →
  health:
    subheader: 🛡️ Model health
    subheader_caption: Health of each active model — error rate, P95 latency, and prediction volume.
    detail_error: 'error: {rate}'
    detail_p95: 'P95: {ms} ms'
    detail_preds: '{count} pred.'
    supervision_link: 🛡️ Full supervision →
    more_versions: … and {count} more version(s)
    no_models: No models available.
  ab_shadow:
    subheader: 🧪 Active experiments
    subheader_caption: Versions running in A/B mode (split traffic) or shadow mode (silent predictions with no user impact).
    ab_info: 🧪 **{count} version(s) in A/B test** — {names}
    shadow_info: 👻 **{count} version(s) in shadow** — {names}
    manage_link: → Manage A/B tests and shadow deployments
  nav:
    subheader: 🗺️ Navigate the dashboard
    subheader_caption: Quick access to all platform features.
    open_btn: Open →
    users_title: Users
    users_desc: Manage accounts, quotas and access requests
    models_title: Models
    models_desc: Upload, version, deploy and compare ML models
    predictions_title: Predictions
    predictions_desc: History, CSV export, batch prediction and observed results
    stats_title: Statistics
    stats_desc: Leaderboard, time evolution and version comparison
    supervision_title: Supervision
    supervision_desc: Global health view, drift detection and real-time alerts
    ab_title: A/B Testing
    ab_desc: Configure progressive deployments and compare variants
    retrain_title: Retraining
    retrain_desc: Cron scheduler, manual retrain, train.py script upload
    golden_title: Golden Tests
    golden_desc: Non-regression test suites with assertions on predictions
    help_title: Help
    help_desc: Documentation, code examples and API integration guide
    autotrain_title: AutoTrain
    autotrain_desc: Generate, test and upload train.py scripts with AI
sidebar:
  account_title: My account
  quota_caption: '{used} / {limit} today'
  quota_exhausted: Quota exhausted for today.
  token_expander: 🔑 My API token
  token_load_error: Could not load the token.
  logout_btn: Log out
  pending_requests: 🔔 {count} access request(s) pending
  manage_requests_link: Manage requests →
  services_title: Services
  credentials_link: 🔑 Access & credentials →
  dark_mode_toggle: Dark mode
auth:
  require_auth_error: You must log in first. Return to the home page.
  require_admin_error: Access restricted to administrators.
ui:
  copy_btn: 📋 Copy token
  copy_success: ✅ Copied!
predictions:
  page_title: 📊 Predictions
  caption: "Full history of predictions made via the API — filter by model, date or status, export to CSV and annotate observed results."
  refresh_btn: 🔄 Refresh
  tab_history: 📋 History
  tab_batch: 📦 Batch predictions
  scripts:
    expander_title: 📋 Example scripts — Iris
    expander_caption: Reference scripts for sending predictions and observed results
      to your Iris model. Run locally after uploading the model.
    iris_desc: Sends 5 Iris observations in single and batch mode (`POST /predict`
      + `POST /predict-batch`)
    ground_truth_desc: Sends real labels (ground truth) to measure the model's actual
      performance (`POST /observed-results`)
    not_found: '# File not found: {filename}'
    dialog_title: Script preview
    download_btn: ⬇️ Download
    view_btn: 👁 View
  coverage:
    expander_title: 🏷️ Ground truth coverage
    total_predictions: Total predictions
    labeled: Labeled
    coverage_rate: Coverage
    by_model: '**By model:**'
    unavailable: Coverage data unavailable.
  filters:
    expander_title: 🔍 Filters
    search_label: Filter by name
    search_placeholder: Search…
    model_label: Model
    all_models: (all)
    all_models_export: (all)
    start_date: Start date
    end_date: End date
    status_label: Status
    status_all: All
    limit_label: Limit
    limit_all: Show all
    conf_min_label: Min confidence
    conf_min_help: Filter predictions with confidence ≥ this threshold (classifiers
      only)
    conf_max_label: Max confidence
    conf_max_help: Filter predictions with confidence ≤ this threshold (classifiers
      only)
    mismatch_only_label: Incorrect predictions only
    mismatch_only_help: Show only rows where the prediction differs from the ground
      truth
    version_label: Version
    version_all: (all)
    result_label: Result (contains)
    result_placeholder: 'e.g. setosa'
    gt_label: Ground truth (contains)
    gt_placeholder: 'e.g. versicolor'
    shadow_label: Shadow
    shadow_all: All
    shadow_only: Shadow only
    shadow_exclude: Exclude shadow
    user_label: User
    user_placeholder: 'e.g. alice'
    id_obs_label: id_obs
    id_obs_placeholder: 'e.g. obs-001'
  warnings:
    no_model: No model available. Create a model via the API first.
    date_order: Start date must be before end date.
  loading:
    progress: Loading… {loaded} / {total}
  errors:
    load_error: 'Error loading data: {error}'
    generic: 'Error: {error}'
  results:
    expander_title: 📋 Results
    filter_note: Results below depend on the filters applied in the 'Filters' expander
      above.
    count_caption: '**{total}** predictions found — {shown} displayed'
    no_predictions: No predictions for these criteria.
    no_mismatch: No incorrect predictions on this page.
  table:
    col_id: ID
    col_id_obs: id_obs
    col_timestamp: Timestamp
    col_model: Model
    col_version: Version
    col_result: Result
    col_ground_truth: Ground Truth
    col_confidence: Confidence
    col_time_ms: Time (ms)
    col_status: Status
    col_shadow: Shadow
    col_user: User
    help_id: Internal identifier automatically assigned to each prediction.
    help_id_obs: Observation identifier provided by the caller. Links a prediction
      to a row in your business system.
    help_timestamp: Exact date and time the prediction was made (UTC).
    help_model: Name of the ML model that responded to this request.
    help_version: Model version used. The production version is used by default.
    help_result: The prediction returned by the model. Red rows = result differs from
      Ground Truth.
    help_ground_truth: True value observed, recorded via /observed-results. '—' if
      not yet recorded.
    help_confidence: Model certainty (max probability). '—' if the model does not
      support probabilities.
    help_time_ms: API-side request processing time in milliseconds.
    help_status: ✅ Success or ❌ Failure during processing.
    help_shadow: '🔮 Shadow: prediction computed in the background, not returned to
      the client. ''—'' = normal prediction.'
    help_user: Identifier of the user or application that made this prediction.
  detail:
    not_found: Prediction not found.
    title: '#### 🔍 Prediction #{pred_id}'
    input_features: '**Input features:**'
    result_label: '**Result:**'
    error_msg: 'Error: {msg}'
    observed_section: '**── Observed result ──**'
    no_id_obs: No `id_obs` — cannot submit an observed result.
    obs_recorded: '✅ Recorded result: **{value}**'
    obs_input_label: Observed value
    obs_input_placeholder: 'E.g.: 0, 1.5, setosa…'
    obs_submit_btn: Save actual result
    obs_empty_warning: Please enter a value.
    shap_section: '**── SHAP explanation ──**'
    shap_btn: 🧠 Compute SHAP explanation
    shap_spinner: Computing SHAP…
    shap_error: 'Unable to compute: {error}'
    shap_prediction_metric: Prediction
    shap_type_metric: Type
    shap_chart_title: SHAP contributions (top 10)
    shap_chart_xaxis: SHAP contribution
    shap_all_features_expander: Show all features
    shap_no_values: No SHAP values returned.
    delete_btn: '🗑️ Delete prediction #{pred_id}'
    delete_toast: 'Prediction #{pred_id} deleted.'
  export:
    expander_title: ⬇️ Export all predictions (server-side)
    caption: The server export includes **all** predictions matching the filters,
      not just the current page.
    format_label: Format
    status_label: Status
    prepare_btn: Prepare export
    spinner: Preparing export…
    download_btn: ⬇️ Download predictions_export.{fmt}
    error: 'Export error: {error}'
  activity:
    expander_title: 👥 Activity by user & model
    caption: Identify the most active users, most requested models, and usage spikes
      that may impact production.
    load_users_error: 'Unable to load users: {error}'
    no_users: No users available.
    spinner: Loading activity statistics…
    no_activity: No activity recorded for the selected period.
    kpi_total_calls: Total calls
    kpi_active_users: Active users
    kpi_active_users_help: Users who made at least 1 prediction in the period.
    kpi_top_user: Top user
    kpi_top_user_delta: '{calls:,} calls ({pct:.0f} % of traffic)'
    kpi_top3_concentration: Top 3 concentration
    kpi_top3_help: Share of total traffic generated by the 3 most active users. A
      high value indicates a strong dependency risk on a few callers.
    summary_header: '#### 📋 Summary by user'
    col_user: User
    col_calls: Calls
    col_traffic_share: Traffic share
    col_models_used: Models used
    col_errors: Errors
    col_error_rate: Error rate
    col_avg_latency: Avg latency
    col_model: Model
    col_calls_help: Total predictions (excluding shadow) in the period.
    col_traffic_share_help: This user's share of total prediction volume.
    col_models_used_help: Number of distinct models called by this user.
    col_error_rate_help: Execution errors (status ≠ 'success') / total calls.
    col_avg_latency_help: Average latency in ms on successful requests.
    chart_top_users_title: Top users by call volume
    chart_daily_title: Daily call trend by user
    chart_date_label: Date
    detail_by_user_model_header: '#### 🔍 Detail by user × model'
    chart_stack_title: Calls by user × model
  csv_import:
    expander_title: 📤 Import observed results (CSV)
    intro: Record the **true observed value** for each prediction to measure the model's
      real accuracy in production (Ground Truth).
    format_header: '**Expected format**'
    format_note: 'Separator: comma  •  Encoding: UTF-8  •  Max size: 10 MB'
    template_download_btn: ⬇️ Download CSV template with examples
    file_uploader_label: CSV file to import
    model_override_label: Force model for all rows (optional)
    model_override_help: 'Leave blank if your CSV already has a ''model_name'' column.


      Fill this field if your CSV has no ''model_name'' column

      or if you want to force a specific model.

      '
    import_btn: Import
    import_success: '{count} results imported from {filename}.'
    skipped_rows: '{count} row(s) skipped'
    import_error: 'Import error: {error}'
    columns_table: '| Column | Required | Description |

      |---|---|---|

      | `id_obs` | No | Unique observation ID (generated if absent) |

      | `model_name` | Yes | Model name |

      | `observed_result` | Yes | Observed value / actual class |

      | `date_time` | No | Observation date/time (ISO 8601 or YYYY-MM-DD) |'
  labeling_queue:
    expander_title: 🏷️ Labeling queue
    caption: Identify which predictions to annotate first to improve model performance.
      Export them as CSV, fill in the `observed_result` column, then import via the
      CSV Import form below.
    strategy_label: Strategy
    strategy_uncertainty: Uncertainty (recommended)
    strategy_recent: Most recent
    strategy_random: Random
    model_label: Model
    all_models: (all)
    limit_label: Number to display
    total_metric: Unlabeled
    total_metric_help: Predictions with no matching observed result (id_obs not yet
      annotated).
    all_labeled: ✅ All predictions have an observed result.
    col_model: Model
    col_version: Version
    col_prediction: Prediction
    col_confidence: Confidence
    col_timestamp: Timestamp
    export_btn: ⬇️ Export {count} prediction(s) to label (CSV)
    import_hint: Fill in the `observed_result` column of the exported CSV, then import
      it via the form below.
    error: 'Error loading the labeling queue: {error}'
    api_code_header: '##### 🐍 Submit ground truth via API'
    api_code_caption: 'Send your labels directly via `POST /observed-results` — useful
      for automated pipeline integration:'
  purge:
    expander_title: 🗑️ GDPR Maintenance — Prediction purge
    caption: Permanently deletes old predictions. Use **Simulate** before confirming.
    days_slider_label: Purge predictions older than
    model_filter_label: Filter by model (optional)
    simulate_btn: 🔍 Simulate (dry_run)
    simulate_result: 'Simulation: **{count}** prediction(s) would be deleted.'
    oldest_remaining: 'Oldest remaining prediction: {ts}'
    models_affected: 'Models affected: {models}'
    linked_obs_warning: ⚠️ {count} linked observed result(s) would be lost.
    simulate_error: 'Simulation error: {error}'
    confirm_btn: ⚠️ Confirm purge
    dialog_title: ⚠️ Confirm permanent purge
    dialog_warning: You are about to **permanently delete** all predictions older
      than **{days} days**.
    dialog_target_model: 'Target model: **{model}**'
    dialog_all_models: All models are targeted.
    dialog_irreversible: This action is **irreversible**.
    dialog_confirm_btn: Confirm deletion
    purge_toast: '{count} prediction(s) deleted.'
    linked_obs_lost: '{count} linked observed result(s) were lost.'
    purge_error: 'Purge error: {error}'
  batch:
    subheader: 📦 Batch scoring — CSV or Parquet
    caption: Upload a file containing your observations, select a model, and download
      the enriched CSV with predictions.
    format_expander_title: ℹ️ Expected file format
    format_description: 'The file must contain **one column per feature** expected
      by the model.

      Columns must match the model''s feature names exactly (case-sensitive).


      An optional `id_obs` column allows each row to be traced in the history.


      **Example for an Iris model:**

      '
    example_download_btn: ⬇️ Download this example (CSV)
    target_model_label: Target model
    version_label: Version
    production_auto: (production / auto)
    file_uploader_label: CSV or Parquet file
    file_uploader_help: CSV (.csv) or Parquet (.parquet). The first row of the CSV
      must be the header.
    file_read_error: 'Unable to read file: {error}'
    file_loaded_caption: 'File loaded: **{filename}** — {rows:,} rows, {cols} columns'
    run_btn: 🚀 Run scoring
    scoring_spinner: Scoring {count:,} observations…
    timeout_error: Scoring exceeded the 120-second timeout. Try a smaller file.
    scoring_error: 'Scoring error: {detail}'
    scoring_toast: '{count:,} predictions generated — model {model} v{version}'
    preview_header: '**Results preview (first 50 rows):**'
    download_full_btn: ⬇️ Download full CSV ({count:,} rows)
    summary_expander_title: 📊 Prediction summary
    summary_count: '- **Number of predictions:** {count:,}'
    stat_mean: Mean
    stat_median: Median
    stat_min: Min
    stat_max: Max
    class_col: Class
supervision:
  page_title: 🔍 Model Supervision
  caption: "Real-time health monitoring of production models: feature drift, anomalies, ROC curve and alert configuration."
  filters:
    from_label: From
    to_label: To
    date_order_error: End date must be after start date.
  loading:
    overview: Loading overview…
    timeseries: Loading time series…
    detail: Loading detail for {model}…
    alerts: Loading configured alerts…
  errors:
    overview_load: 'Unable to load overview: {error}'
    detail_load: 'Unable to load detail: {error}'
    no_data: No predictions in the selected period.
    generic: 'Error: {error}'
  kpis:
    predictions_prod: Prod. predictions
    predictions_shadow: Shadow pred.
    error_rate: Error rate
    avg_latency: Avg latency
    active_models: Active models
    alerts: Alerts
    alerts_delta_issues: '{count} model(s) to monitor'
    alerts_delta_ok: All good
  tab_global: 🌍 Global view
  tab_detail: 🔎 Zoom on a model
  global:
    export_csv_btn: 📥 Export CSV
    export_csv_help: Download supervision metrics in CSV format
    export_md_btn: 📄 Export Markdown
    export_md_help: Download a narrative summary of alerts and recommendations
    alerts_expander: 🔔 Configured alerts
    alerts_caption: "Versions that have at least one alert threshold defined. Only
      the production version is shown per model."
    alerts_col_model: Model
    alerts_col_version: Version
    alerts_col_mode: Mode
    alerts_col_error_max: Max error rate
    alerts_col_accuracy_min: Min accuracy
    alerts_col_auc_min: Min AUC
    alerts_col_drift: Auto drift
    alerts_col_webhook: Webhook
    alerts_none: No model has alert thresholds configured.
    alerts_manage_header: '**Manage thresholds**'
    alerts_select_label: Select a version
    alerts_disable_btn: ⬜ Disable alerts
    alerts_disable_help: "Disables automatic notifications (drift_auto_alert=false)\
      \ while keeping the configured thresholds."
    alerts_delete_btn: 🗑️ Delete thresholds
    alerts_delete_help: Deletes all alert thresholds for this version.
    alerts_disabled_ok: ✅ Alerts disabled for {model} v{version}.
    alerts_deleted_ok: ✅ Thresholds deleted for {model} v{version}.
    alerts_create_header: '**➕ Configure thresholds for a version**'
    alerts_create_select: Model and version to configure
    health_subheader: 🏥 Health by model
  health_table:
    col_model: Model
    col_versions: Versions
    col_mode: Mode
    col_nb_prod: "Nb prod. versions"
    col_nb_shadow: "Nb shadow versions"
    col_nb_active: "Nb active versions"
    col_predictions: Predictions
    col_shadow: Shadow
    col_errors: Errors
    col_avg_latency: Avg latency
    col_p95: p95
    col_drift_features: Feature drift
    col_drift_perf: Perf. drift
    col_drift_output: Output drift
    col_status: Status
    help_model: Name of the deployed ML model.
    help_versions: Active versions (is_active=True).
    help_nb_prod: Number of versions currently in production mode (🟢).
    help_nb_shadow: Number of versions currently in shadow mode (🟣).
    help_nb_active: Total number of active versions (all versions with is_active=True).
    help_mode: 'Deployment mode(s) of active versions:

      🟢 Production · 🟠 A/B · 🟣 Shadow · ⚪ — (no routing)'
    help_predictions: Production predictions returned to clients (is_shadow=False)
      in the period.
    help_shadow: Silent predictions (is_shadow=True) computed in the background, not
      returned to the client.
    help_errors: 'Execution error rate: COUNT(status ≠ ''success'') / COUNT(*)


      ⚠️ Server errors only. This is NOT an ML quality indicator.


      🟡 Warning: ≥ 5 %

      🔴 Critical: ≥ 10 %

      '
    help_avg_latency: Average response time (ms) on successful requests in the period.
    help_p95: '95th percentile latency: 95 % of successful requests processed in less
      than this time.'
    help_drift_features: 'Drift detection on each numeric feature.

      Status = worst among 3 metrics (Z-score, PSI, null rate).


      🟡 Warning · 🔴 Critical

      ⚪ no_baseline = no baseline stored.

      ⚪ insufficient_data = fewer than 10 predictions.

      '
    help_drift_perf: 'Performance comparison between the 1st and 2nd half of the period.


      🟡 Warning: drop ≥ 5 points

      🔴 Critical: drop ≥ 10 points

      ⚪ no_data = insufficient data.

      '
    help_drift_output: 'Drift of the model''s predicted value distribution (PSI).


      🟡 Warning: PSI ≥ 0.1

      🔴 Critical: PSI ≥ 0.2

      ⚪ no_baseline = no label_distribution stored.

      '
    help_status: 'Overall health status = worst of 4 indicators:

      ① Execution error rate

      ② Feature drift

      ③ Performance drift

      ④ Output drift


      🟢 ok · 🟡 warning · 🔴 critical

      '
  charts:
    pie_title: Production prediction distribution
    error_rate_ts_title: Execution error rate trend (%)
    latency_ts_title: Average response latency trend (ms)
    date_label: Date
    errors_pct_label: Errors (%)
    model_label: Model
    latency_avg_label: Avg latency (ms)
    warning_5pct: Warning 5 %
    critical_10pct: Critical 10 %
    no_timeseries_data: No time series data available
    no_latency_data: No latency data available
    vol_ts_title: Predictions & errors per day
    count_label: Count
    predictions_trace: Predictions
    errors_trace: Errors
    latency_day_title: Latency per day (ms)
    latency_mean_trace: Average
    error_rate_day_title: Error rate per day
    error_rate_label: Error rate
  detail:
    search_label: Filter by name
    search_placeholder: Search for a model…
    select_model_label: Select a model
    link_manage_models: 🤖 Manage in Models
    link_retrain: 🔄 Retrain
    versions_expander: 📋 Versions & Traffic
    activity_expander: 📈 Activity & Latency
    drift_expander: 🌡️ Drift & Anomalies  {icon} {status}
    ab_expander: ⚖️ A/B & Shadow comparison
    calibration_expander: 📏 Probability calibration
    recent_errors_expander: 🔔 Recent errors ({count})
    no_version_active: No active version in the period.
    no_timeseries: No time series available for the period.
  versions_table:
    col_version: Version
    col_mode: Mode
    col_traffic_weight: Traffic weight
    col_predictions: Predictions
    col_shadow: Shadow
    col_errors: Errors
    col_avg_latency: Avg latency
    col_p50: p50
    col_p95: p95
    help_version: Version number.
    help_mode: production · ab_test · shadow.
    help_traffic_weight: Traffic share in A/B mode.
    help_predictions: Production predictions in the period.
    help_shadow: Shadow predictions in the period.
    help_errors: Error rate for this version.
    help_avg_latency: Average response time (ms).
    help_p50: Median response time.
    help_p95: 95th percentile — SLA indicator.
  drift:
    perf_drift_header: '**📈 Performance drift**'
    perf_drift_note: "Compares model performance (accuracy, R²…) between the first
      and second half of the period. A drop ≥ 5 pts triggers a warning, ≥ 10 pts
      a critical. Requires observed results submitted via /observed-results."
    feat_drift_header: '**🌡️ Feature drift**'
    feat_drift_note: "Detects whether input feature values have shifted relative to
      the training baseline. A feature drift may indicate an upstream pipeline change
      or a population shift."
    metric_r2_day: R² / day
    metric_mae_day: MAE / day
    metric_accuracy_day: Accuracy / day
    delta_fmt_r2: '{val} (1st vs 2nd half)'
    delta_fmt_mae: '{val} (1st vs 2nd half)'
    delta_fmt_accuracy: '{val} (1st vs 2nd half)'
    rolling_3d_trace: 3-day avg
    status_critical: 🔴 Critical
    status_warning: 🟡 Warning
    status_stable: 🟢 Stable
    perf_trend_metric: Performance trend
    no_obs_in_period: No linked observations in the period.
    no_perf_data: No performance data available.
    global_status: '**Global status**: {icon} `{status}`'
    predictions_analyzed: '{count} predictions analyzed'
    no_baseline: No feature baseline recorded for this model.
    no_drift_data: No drift data available.
    insufficient_data: Insufficient data ({count} observations — threshold 30).
    cat_drift_header: '**📊 Categorical feature drift**'
  drift_cat:
    col_category: Category
    col_baseline: Baseline (%)
    col_production: Production (%)
    col_delta: Δ (pts)
    y_axis: Frequency (%)
    legend_baseline: Baseline
    legend_production: Production
  drift_table:
    col_feature: Feature
    col_status: Status
    col_prod_mean: Prod mean
    col_baseline_mean: Baseline mean
    col_z_score: Z-score
    col_psi: PSI
    col_n_prod: N prod.
    help_feature: Name of the model's input feature.
    help_status: Feature drift status — worst among Z-score, PSI and null rate.
    help_prod_mean: Average values received in production over the selected period.
    help_baseline_mean: Average of this feature in the training dataset.
    help_z_score: Z = |μ_prod − μ_baseline| / σ_baseline. 🟡 ≥ 2 · 🔴 ≥ 3
    help_psi: PSI = Population Stability Index. 🟡 ≥ 0.1 · 🔴 ≥ 0.2
    help_n_prod: Number of recent predictions used to compute production statistics.
    zscore_psi_legend: "**How to interpret these columns?**\n\n\
      • **Z-score** = \\|μ_prod − μ_baseline\\| / σ_baseline: measures the gap in\
      \ standard deviations between the production mean and the training mean. 🟡\
      \ ≥ 2 (warning) · 🔴 ≥ 3 (critical). A high Z-score means the received value\
      \ distribution has significantly shifted from what the model saw during training.\n\n\
      • **PSI** (Population Stability Index): measures the overall shape of the distribution\
      \ (not just the mean). 🟡 ≥ 0.1 (moderate change) · 🔴 ≥ 0.2 (major change).\
      \ A high PSI suggests the data population has fundamentally changed."
  confidence:
    header: '**📉 Confidence trend**'
    note: "Evolution of the maximum probability returned by the model (classifiers
      only). A declining confidence over time may signal input data drift or a model
      reaching its generalization limit. The blue band represents the interquartile
      range (P25–P75)."
    no_probabilities: This model does not return probabilities — confidence trend
      unavailable.
    delta_str: '{val} (1st vs 2nd half)'
    mean_metric: Mean confidence
    mean_trace: Mean confidence
    threshold_50: Threshold 50%
    y_label: Confidence
    low_rate_warning: '{pct} % of predictions below the threshold — possible input
      drift signal.'
    unavailable: Confidence trend unavailable.
  anomalies:
    header: '**🚨 Anomalous predictions**'
    caption: 'A prediction is marked **anomalous** when at least one of its input
      features

      has a Z-score ≥ threshold relative to training data.

      '
    note: "Use this table to identify outlier requests sent to your model. Adjust
      the Z threshold (slider) to broaden or narrow detection. A high Z-score on
      a feature indicates a value very unusual compared to the training distribution.
      Click a prediction to drill down feature by feature."
    window_label: Window (days)
    z_threshold_label: Z-score threshold
    z_threshold_help: Detection threshold for anomalous features. Z = |received value
      − μ_train| / σ_train ≥ threshold.
    unavailable: 'Anomalous predictions unavailable: {error}'
    no_baseline: No feature baseline — cannot compute Z-scores.
    metric_analyzed: Analyzed predictions
    metric_analyzed_help: Recent predictions analyzed over the last {days} days.
    metric_anomalous: Anomalous predictions
    metric_anomalous_help: Predictions with at least one feature where |Z| ≥ {z:.1f}.
    metric_rate: Anomaly rate
    metric_rate_help: Share of anomalous predictions among analyzed predictions.
    none_detected: No anomaly detected (z ≥ {z:.1f}) on {total} prediction(s).
    detail_header: '##### 🔍 Prediction detail'
    inspect_selectbox: Prediction to inspect
    inspect_placeholder: Select a prediction…
    pred_header: 'Prediction #{pred_id}'
    ground_truth_help: Actual value observed (via observed_results).
    z_scores_caption: Z-scores of anomalous features
    threshold_annotation: Threshold {z:.1f}
    values_vs_baseline_caption: Received values vs training baseline
  anomaly_table:
    col_id: ID
    col_timestamp: Timestamp
    col_result: Result
    col_ground_truth: Ground truth
    col_z_max: Max Z-score
    col_anomalous_features: Anomalous features
    col_confidence: Confidence
    help_id: Unique prediction identifier in the database.
    help_timestamp: Date and time of the prediction.
    help_result: Value predicted by the model for this request.
    help_ground_truth: True value observed (if an observed_result was submitted).
    help_id_obs: Business identifier of the observation.
    help_z_max: Highest Z-score among all anomalous features of this prediction.
    help_anomalous_features: Features where |Z| ≥ threshold, sorted by descending
      Z-score.
    help_confidence: Max probability returned by the model (classifiers only).
  feat_detail_table:
    col_feature: Feature
    col_received_value: Received value
    col_train_mean: Train mean (μ)
    col_train_mean_short: μ train
    col_train_std: Std dev (σ)
    col_train_std_short: σ train
    col_normal_range: Normal range
    col_normal_range_label: Normal range [μ±2σ]
    col_z_score: Z-score
    help_received_value: Value received in the prediction request.
    help_train_mean: Mean of this feature in the training dataset.
    help_train_std: Standard deviation of this feature in the training dataset.
    help_normal_range: Interval μ ± 2σ — 95 % of training values fell within this
      range.
    help_z_score: Z = |received value − μ| / σ
  ab_table:
    col_version: Version
    col_predictions: Predictions
    col_shadow: Shadow
    col_error_rate: Error rate
    col_avg_latency: Avg latency
    col_p95: p95
    col_shadow_agreement: Shadow agreement
    help_version: Model version number.
    help_predictions: Predictions returned to clients (is_shadow=False) in the period.
    help_shadow: Silent predictions computed in the background (is_shadow=True).
    help_error_rate: Proportion of technically failed requests. 🟡 ≥ 5 % · 🔴 ≥ 10 %
    help_avg_latency: Average API response time for this version (ms).
    help_p95: 95th percentile latency — key service quality indicator.
    help_shadow_agreement: Agreement rate between this version and the production
      model.
  ab_regression:
    header: '**📊 Predicted output distribution — regression (prod + shadow)**'
    col_mean_y: Mean ŷ
    col_std: Std dev σ
    col_min: Min
    col_max: Max
    col_n_label: N (prod + shadow)
    y_axis_label: Predicted value ŷ
    chart_title: Mean ŷ ± σ by version
    help_version: Model version number.
    help_n: Total predictions analyzed for this version (production + shadow).
    help_mean_y: Average predicted value across all requests (prod + shadow).
    help_std: Dispersion of predictions around the mean.
    help_min: Lowest predicted value observed in the period.
    help_max: Highest predicted value observed in the period.
  ab_classification:
    header: '**📊 Predicted class distribution (prod + shadow)**'
    col_total: Total (prod+shadow)
    class_label: Class
    pct_label: Share (%)
    predicted_class_label: Predicted class
    chart_title: Predicted class distribution — prod + shadow (%)
    class_col_label: Class {lbl}
    class_col_help: Share of predictions classified as **{lbl}** out of the total
      (prod + shadow).
    help_version: Model version number.
    help_total: Total predictions analyzed (production + shadow).
    hover_template: Class %{fullData.name}<br>%{y:.1f}%  (%{customdata[0]} prod+shadow
      predictions)<extra></extra>
  calibration:
    unavailable: 'Calibration unavailable: {error}'
    regression_insufficient: 'Submit observed_results to activate regression calibration
      (sample: {count} pairs).'
    regression_caption: 'Regression model calibration measures how closely the predicted
      values ŷ match the real values y.

      The ŷ vs y scatter plot should cluster around the diagonal (perfect calibration).

      Residuals (ŷ − y) should be centered at 0 with no systematic bias.

      '
    status_ok: 🟢 OK
    status_biased_high: 🟡 Over-estimation
    status_biased_low: 🟡 Under-estimation
    status_overconfident: 🟡 Overconfident
    status_underconfident: 🔴 Underconfident
    mean_bias_metric: Mean bias
    perfect_calib_trace: Perfect calibration (ŷ = y)
    predictions_trace: Predictions
    scatter_hover: 'True y: %{x:.3f}<br>Predicted ŷ: %{y:.3f}<extra></extra>'
    scatter_title: Regression calibration — predicted ŷ vs true y (n={n})
    scatter_x: True y (observed)
    scatter_y: Predicted ŷ
    bias_annotation: Mean bias = {val:+.3f}
    residuals_trace: Residuals (ŷ − y)
    residuals_hover: 'Residual: %{x:.3f}<br>Count: %{y}<extra></extra>'
    residuals_title: Residual distribution (ŷ − y)
    residuals_x: Residual (ŷ − y)
    residuals_y: Number of predictions
    overestimates: overestimates
    underestimates: underestimates
    bias_warning: The model **{direction}** its predictions systematically. Consider
      retraining or post-processing calibration.
    classification_insufficient: 'Submit observed_results to activate calibration
      (sample: {count} pairs).'
    brier_metric: Brier score
    gap_metric: Confidence/accuracy gap
    status_metric: Status
    observed_rate_trace: Observed rate
    reliability_hover: 'Bucket: %{x}<br>Observed rate: %{y:.2%}<br>N: %{customdata}<extra></extra>'
    reliability_title: Calibration curve (reliability diagram)
    reliability_x: Predicted confidence
    reliability_y: Observed rate
    overconfident_advice: Consider `CalibratedClassifierCV(method='isotonic')` at
      the next retrain.
  config:
    expander_base_label: ⚙️ Configuration
    tab_thresholds: 🔔 Alert thresholds
    tab_auto_promote: 🚀 Auto-promotion
    tab_circuit_breaker: ⚡ Circuit breaker
    no_version: No version available.
    version_to_configure: Version to configure
    threshold_error_rate_max: Max error rate
    threshold_accuracy_min: Min accuracy
    threshold_auc_min: Min AUC
    threshold_drift_alert: Drift alert
    drift_alert_enabled: ✅ Enabled
    drift_alert_disabled: ⬜ Disabled
    thresholds_note: "**What these thresholds trigger:**\n\n\
      Alert thresholds color the model's health status in the global table (🟡 warning\
      \ / 🔴 critical) and appear as reference lines on this page's charts. The **Drift\
      \ alert** checkbox enables automatic notifications when drift is detected on\
      \ this model.\n\n\
      **Available notification channels:**\n\n\
      • **Webhook (per model)** — set `webhook_url` directly on the model version\
      \ via `PATCH /models/{name}/{version}` with `{\"webhook_url\": \"https://hooks.slack.com/…\"}`.\
      \ The API sends a JSON POST to this URL on each alert.\n\n\
      • **Email (global)** — configure these environment variables in the API container:\
      \ `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`, `SMTP_STARTTLS`\
      \ and `ALERT_EMAIL_TO` (recipient address(es), comma-separated). Restart the\
      \ container after any change."
    form_error_rate_max: Max error rate (%)
    form_error_rate_help: Triggers an alert if the error rate exceeds this threshold.
    form_accuracy_min: Minimum accuracy
    form_accuracy_help: Triggers an alert if accuracy falls below this threshold.
    form_auc_min: Minimum AUC (binary classifier)
    form_auc_min_help: "Triggers an alert if the model's stored AUC falls below this
      threshold. Visible only for binary classification models. The AUC compared is
      the one recorded during the last training or retrain."
    form_drift_auto_alert: Automatic alert on feature drift
    form_drift_auto_alert_help: "If checked, the supervision job (every 6 h) sends an\
      \ alert (email + webhook) as soon as a critical drift is detected on the production\
      \ model's input features. Uncheck to silence alerts without deleting the other\
      \ thresholds."
    form_save_btn: 💾 Save thresholds
    thresholds_saved: ✅ Thresholds updated for {model} v{version}.
    auto_promote_caption: "**Auto-promotion** — promotes the retrained model automatically if all quality criteria are met. Applied to all versions of **{model}**. Setting 0 on a criterion disables it."
    auto_promote_context: "This form configures the same policy as **Models → ⚙️ Promotion policy**.\n\nThe evaluation runs **after each retrain** (manual or scheduled). Results are visible in the Retrain page → History column.\n\n> ℹ️ Circuit breaker settings (auto-demotion) are configured in the **Models** page."
    auto_promote_enable: Enable auto-promotion
    min_accuracy_label: Minimum accuracy (0 = disabled)
    min_accuracy_help: "Accuracy required on recent (prediction → observed result) pairs to approve promotion. Example: 0.90 → the new version must be correct on at least 90 % of validation cases."
    min_golden_label: Min Golden Tests pass rate (0 = disabled)
    min_golden_help: "Proportion of golden tests that must pass. Configure golden tests in the Golden Tests page."
    max_mae_label: Maximum MAE (0 = disabled)
    max_mae_help: "Maximum allowed Mean Absolute Error (regression models only). Example: 5.0 → average error must not exceed 5 units."
    max_latency_label: Max P95 latency (ms, 0 = disabled)
    max_latency_help: "95th-percentile response time must stay below this value. Example: 200 → 95 % of predictions must complete in under 200 ms."
    min_samples_label: Min validation pairs
    min_samples_help: "Minimum number of (prediction, observed result) pairs required for a reliable evaluation. If insufficient ground-truth data is available, auto-promotion is skipped."
    criteria_summary_header: "**✅ Active criteria (all must be satisfied):**"
    criteria_accuracy: "Accuracy ≥ {val:.2f}"
    criteria_mae: "MAE ≤ {val}"
    criteria_latency: "P95 latency ≤ {val:.0f} ms"
    criteria_samples: "At least {val} validation pairs"
    criteria_golden: "Golden Tests pass rate ≥ {val:.2f}"
    criteria_none: "⚠️ Auto-promotion enabled but **no quality criterion configured** — the new version will be promoted after any retrain without quality checks."
    save_auto_promote_btn: 💾 Save auto-promotion
    auto_promote_saved: Auto-promotion policy updated.
    admin_only: 🔒 Administrators only.
    circuit_breaker_caption: Automatically removes from production a model whose performance
      degrades.
    circuit_breaker_enable: Enable circuit breaker
    demote_drift_trigger: Drift trigger level
    drift_level_warning: ⚠️ Warning
    drift_level_critical: 🔴 Critical
    demote_accuracy_label: Minimum accuracy (0 = disabled)
    demote_accuracy_help: Falls below this threshold → automatic demotion.
    cooldown_label: Cooldown (hours)
    cooldown_help: Minimum delay between two auto-demotions.
    save_circuit_breaker_btn: 💾 Save circuit breaker
    circuit_breaker_saved: Circuit breaker updated.
    last_demote_error: 🔴 **Auto-demoted on {ts} UTC** — {reason}
    no_recent_demote: 🟢 No recent auto-demotion detected.
    cb_status_label: "Circuit Breaker Status"
    cb_status_active_value: "🔴 ACTIVE"
    cb_status_inactive_value: "⬜ INACTIVE"
    cb_intro_panel: "The **circuit breaker** automatically monitors this model every\
      \ **6 hours** (via the `retrain-worker` container). If it detects critical drift\
      \ or accuracy dropping below the threshold, it removes the model from production\
      \ (`is_production=false`).\n\n📋 **Prerequisites**: a **fallback** version (non-production,\
      \ active) must exist. The **cooldown** prevents rapid oscillations."
    cb_trigger_summary_drift: "Drift trigger: **{level}**"
    cb_trigger_summary_acc: "Min accuracy: **{val}**"
    cb_trigger_summary_acc_disabled: "Min accuracy: ⬜ disabled"
    cb_trigger_summary_cooldown: "Cooldown: **{hours} h**"
access_request:
  title: 📝 PredictML Access Request
  intro: Fill in this form to request an account. An administrator will review your
    request and send you your access token.
  username_label: Desired username *
  username_placeholder: john_doe
  username_help: Between 3 and 50 characters.
  email_label: Email *
  email_placeholder: john@example.com
  role_label: Desired access type
  role_user: User (can make predictions)
  role_readonly: Read only
  message_label: Message to the administrator (optional)
  message_placeholder: Describe your use case, your team, etc.
  submit_btn: Submit my request
  error_required: Username and email are required.
  error_username_short: Username must be at least 3 characters.
  success: ✅ Your request has been recorded. An administrator will review it and contact
    you directly to send you your access token.
  reference: 'Your request reference: **#{id}**'
  error_pending: A request is already pending for this email. Please wait for an administrator
    to process it.
  error_exists: An account already exists with this email or username. If you lost
    your token, contact an administrator.
  error_invalid: 'Invalid data: check the email and username.'
  error_rate_limit: Too many requests submitted recently. Please try again in a few
    moments.
  error_unexpected: Unexpected error ({code}). Please try again.
  error_connection: Unable to reach the API ({api_url}). Check the URL or contact
    an administrator.
  error_generic: 'Error: {error}'
  already_have_token: Already have a token? [Log in on the home page](/) or use the
    application directly via the API.
services:
  page_title: Services — PredictML
  title: 🔌 Services & Access
  caption: Quick access to infrastructure administration interfaces — administrators
    only
  missing_vars_warning: "⚠️ Missing environment variable(s): {vars}.  \nSet them in\
    \ your `.env` file and restart the Streamlit container."
  show_secrets_toggle: 🔓 Show passwords
  grafana_desc: Monitoring, dashboards and alerts — LGTM stack (Loki · Grafana · Tempo
    · Prometheus)
  minio_desc: S3 object storage — `.joblib` models, MLflow artifacts, training datasets
  mlflow_desc: Experiment tracking — runs, metrics, artifacts, comparisons and model
    registry
  open_btn: Open {name} →
  swagger_name: 📄 API Swagger
  swagger_desc: Interactive FastAPI documentation — test endpoints directly from the browser using your token.
  swagger_token_header: "**Bearer Token**"
  swagger_token_caption: Paste this token into the Swagger `Authorize` field to test authenticated endpoints.
  swagger_token_missing: Token not available — please log in to the dashboard again.
  swagger_env_caption: 'Env var: `API_PUBLIC_URL`'
  credentials_header: '**Credentials**'
  login_label: Login
  password_label: Password
  password_missing: Variable `{env_var}` not defined in `.env`
  url_header: '**Service URL**'
  env_var_caption: 'Env var: `{env_url}`'
  config_expander: ℹ️ Configuration & security
  config_body: '**How to configure public URLs?**


    These links use the **public URLs** of the services (accessible from your browser),

    distinct from the internal Docker URLs. Set them in your `.env`:


    | Variable | Default | Description |

    |---|---|---|

    | `GRAFANA_PUBLIC_URL` | `http://localhost:3000` | Grafana interface |

    | `MINIO_CONSOLE_PUBLIC_URL` | `http://localhost:9011` | MinIO console |

    | `MLFLOW_PUBLIC_URL` | `http://localhost:5000` | MLflow interface |

    | `GRAFANA_ADMIN_USER` | `admin` | Grafana login |

    | `GRAFANA_ADMIN_PASSWORD` | *(generated)* | Grafana password |

    | `MINIO_ROOT_USER` | `minioadmin` | MinIO login |

    | `MINIO_ROOT_PASSWORD` | *(generated)* | MinIO password |

    | `MLFLOW_ADMIN_USER` | `admin` | MLflow login |

    | `MLFLOW_ADMIN_PASSWORD` | *(generated)* | MLflow password |

    | `ANTHROPIC_API_KEY` | *(empty)* | Anthropic API key — enables the AI chatbot |

    | `ENABLE_EMAIL_ALERTS` | `false` | Enable email alerts |

    | `SMTP_HOST` | *(empty)* | SMTP server (e.g. smtp.gmail.com) |

    | `SMTP_PORT` | `587` | SMTP port |

    | `SMTP_STARTTLS` | `true` | STARTTLS encryption |

    | `SMTP_USER` | *(empty)* | SMTP username |

    | `SMTP_PASSWORD` | *(empty)* | SMTP password |

    | `SMTP_FROM` | *(empty)* | Sender address |

    | `ALERT_EMAIL_TO` | *(empty)* | Alert recipients (comma-separated) |

    | `WEEKLY_REPORT_ENABLED` | `false` | Enable weekly report |

    | `WEEKLY_REPORT_DAY` | `monday` | Report send day |

    | `WEEKLY_REPORT_HOUR` | `8` | Send hour (UTC) |

    | `PERFORMANCE_DRIFT_ALERT_THRESHOLD` | `0.10` | Performance drop threshold (10%) |

    | `ERROR_RATE_ALERT_THRESHOLD` | `0.10` | Error rate threshold (10%) |


    Secrets are automatically generated by `scripts/init_env.sh` on first deployment.


    **Security:** this page is accessible only to users with the **admin** role.

    Passwords are hidden by default — enable the toggle to copy them.

    '
  config_section_title: External service configuration
  config_section_caption: Environment variables to set in `.env` to enable optional
    features (alerts, AI chatbot).
  smtp_section_title: "📧 Email alerts (SMTP)"
  smtp_section_caption: Configuration for sending alerts and reports by email. Managed
    by the ARQ worker (`retrain-worker` container).
  smtp_status_enabled: "✅ Email alerts active"
  smtp_status_disabled: "ℹ️ Email alerts disabled — `ENABLE_EMAIL_ALERTS=false`"
  smtp_not_configured: "⚠️ SMTP not configured — set `SMTP_HOST` in `.env` to enable\
    \ alerts."
  smtp_host_label: SMTP server
  smtp_port_label: Port
  smtp_tls_help: STARTTLS encrypts the connection after the initial handshake (recommended
    on port 587).
  smtp_user_label: SMTP username
  smtp_from_label: Sender address (FROM)
  smtp_password_label: SMTP password
  smtp_to_label: Alert recipients (TO)
  smtp_perf_threshold_label: Perf. drift threshold
  smtp_perf_threshold_help: An alert is sent if accuracy drops by more than this percentage
    between two consecutive periods.
  smtp_error_threshold_label: Error rate threshold
  smtp_error_threshold_help: An alert is sent if the prediction error rate exceeds
    this threshold.
  smtp_weekly_report: Weekly report
  smtp_weekly_report_schedule: "Every {day} at {hour}:00 UTC"
  smtp_weekly_report_disabled: Disabled
  anthropic_section_title: "🤖 AI Chatbot (Anthropic)"
  anthropic_section_caption: Anthropic API key used by the **AutoTrain Chatbot** page
    to generate `train.py` scripts and interact with the database.
  anthropic_key_label: API key
  anthropic_key_missing: "⚠️ `ANTHROPIC_API_KEY` not set — the Chatbot page will be\
    \ unavailable."
  anthropic_key_set: "✅ Anthropic API key configured"
  anthropic_model_label: Model in use
  anthropic_model_caption: "Claude model read from the `ANTHROPIC_MODEL` environment\
    \ variable (`.env`). Default: `claude-sonnet-4-6`."
golden_tests:
  page_title: Golden Tests — PredictML
  title: 🧪 Golden Tests
  caption: Validate that a model always produces the expected outputs on reference
    cases. Particularly useful after retraining.
  error_load_models: 'Unable to load models: {error}'
  no_models: No model available.
  section_select_model: 1. Model selection
  model_label: Model
  version_label: Version to test
  section_existing_tests: 2. Registered test cases
  error_load_tests: 'Unable to load golden tests: {error}'
  no_tests: No test cases registered for **{name}**. Use the form below to add some.
  col_id: ID
  col_id_help: Unique identifier of the test case in the database.
  col_description: Description
  col_description_help: Free label describing this test case.
  col_input_features: Input features
  col_input_features_help: JSON object containing feature values sent to the model
    during the test.
  col_expected_output: Expected output
  col_expected_output_help: Expected model output for these features.
  col_date: Date
  col_date_help: Date and time this test case was recorded.
  col_created_by: Created by
  col_created_by_help: Username of the admin who created this test case.
  tests_count: '{count} test case(s) registered.'
  run_all_btn: ▶ Run all tests on v{version}
  delete_expander: 🗑️ Delete a test case
  delete_select_label: Test case to delete
  delete_btn: Delete
  delete_success: 'Test case #{id} deleted.'
  delete_error: 'Error deleting: {error}'
  run_spinner: Running {count} test(s) on {name} v{version}…
  metric_total: Total
  metric_passed: ✅ Passed
  metric_failed: ❌ Failed
  metric_pass_rate: Pass rate
  no_details: No details returned by the API.
  detailed_results_header: '#### Detailed results'
  test_id_label: 'Test #{id}'
  expected_label: '**Expected**'
  received_label: '**Received**'
  diff_label: '**Diff**'
  diff_content: '`expected` → **{expected}** | `received` → **{actual}**'
  input_used_label: Input used (JSON)
  run_error: 'Error running tests: {error}'
  section_add_test: 3. Add a test case
  admin_only: Section restricted to administrators.
  add_expander: ➕ New test case
  form_description_label: Description
  form_description_placeholder: 'ex: typical {name} — nominal feature values'
  form_features_label: Input features (JSON)
  form_features_help: JSON key/value object of model features
  form_expected_label: Expected output
  form_expected_placeholder: 'e.g.: setosa  |  0  |  1.23'
  form_expected_help: Expected model output for these features (free text)
  form_save_btn: 💾 Save
  error_invalid_json: 'Invalid JSON in features: {error}'
  error_description_required: Description is required.
  error_expected_required: Expected output is required.
  error_features_empty: Input features cannot be empty.
  error_expected_not_in_classes: 'Expected output must be one of the model''s known classes: {classes}.'
  save_success: Test case saved successfully.
  save_error: 'Error saving: {error}'
  csv_expander: 📥 CSV Import (batch tests)
  csv_format_desc: 'Import multiple test cases at once from a CSV file.


    **Expected format:**

    ```

    description,input_features,expected_output

    "iris setosa","{""sepal_length"": 5.1, ""sepal_width"": 3.5}",setosa

    ```


    Columns: `description` (optional), `input_features` (stringified JSON), `expected_output`.

    '
  csv_format_intro: "Import multiple test cases at once from a CSV file.\n\n**Expected format:**"
  csv_format_columns: "Feature columns: {cols} + `expected_output` (required) + `description` (optional)."
  csv_uploader_label: CSV file
  csv_import_btn: 📤 Import
  csv_import_success: '{count} test case(s) imported.'
  csv_import_error: 'Error during import: {error}'
users:
  page_title: Users — PredictML
  title: 👥 User Management
  caption: "Manage user accounts: roles, prediction quotas, API tokens and pending access requests. Administrators only."
  tab_users: 👥 Users
  tab_requests: 📋 Access Requests
  my_profile: My profile
  profile_username_role: '**User:** {username}  |  **Role:** {role}'
  profile_email: '**Email:** {email}'
  profile_api_token: '**API Token:**'
  profile_daily_quota: '**Daily quota:**'
  quota_unlimited: Unlimited quota.
  quota_tooltip: '{used} / {limit} predictions used today — resets at midnight UTC'
  quota_caption: '{used} / {limit} predictions used today — {remaining} remaining
    — resets at midnight UTC'
  regen_token_expander: ⚠️ Regenerate my token
  regen_token_warning: Your current token will be **immediately invalidated**. Copy
    the new token before closing this page.
  regen_token_btn: 🔄 Regenerate now
  regen_token_success: New token generated — save it now!
  error_load_profile: 'Unable to load profile: {error}'
  error_load_users: 'Unable to load users: {error}'
  error_generic: 'Error: {error}'
  no_users: No users found.
  status_active: ✅ Active
  status_inactive: ❌ Inactive
  expander_list: 👥 User list
  expander_actions: ⚙️ Actions
  expander_create: ➕ Create a new user
  expander_analytics: 📊 Usage analytics — {username}
  select_user: Select a user
  select_change_role: Change role
  btn_deactivate: Deactivate
  btn_activate: Activate
  btn_renew_token: 🔄 Renew token
  btn_apply_role: ✏️ Apply role
  btn_delete: 🗑️ Delete
  btn_confirm_delete: Yes, delete
  btn_cancel: Cancel
  btn_clear_token: Clear token display
  toast_deactivated: User deactivated.
  toast_activated: User activated.
  toast_token_generated: New token generated.
  toast_role_updated: Role updated → {role}
  toast_created: User {username} created.
  toast_deleted: User deleted.
  new_token_info: 'New token — send it manually to the user:'
  confirm_delete_warning: Confirm deletion of **{username}** and all their predictions?
  form_username: Username
  form_email: Email
  form_role: Role
  form_quota: Daily quota
  form_submit: Create
  form_error_required: Username and email are required.
  info_save_token: Save this token — it won't be shown again!
  analytics_quota_today: Quota consumption (today)
  analytics_quota_delta: '{calls} / {limit} calls'
  analytics_date_start: Start date
  analytics_date_end: End date
  analytics_date_error: Start date must be before end date.
  role:
    user: user
    admin: admin
    readonly: readonly
  col:
    id: ID
    id_help: Unique user identifier in the database.
    username: User
    username_help: Username (login). Used to authenticate with the API.
    email: Email
    email_help: User's email address.
    role: Role
    role_help: 'Access level: ''admin'' can do everything, ''user'' can predict, ''readonly''
      can only view.'
    status: Status
    status_help: '✅ Active: access granted. ❌ Inactive: access revoked.'
    quota: Quota / day
    quota_help: Maximum number of predictions allowed per day. Resets at midnight
      UTC.
    last_login: Last login
    last_login_help: Date and time of last API usage.
    created: Created on
    created_help: Date on which the account was created.
  analytics:
    subheader_by_model: Predictions by model
    col_model: Model
    col_volume_total: Total volume
    col_max_day: Max / day
    col_avg_day: Avg / day
    col_median_day: Median / day
    col_errors: Errors
    col_total_label: Total
    no_predictions: No predictions in the selected period.
    no_daily_data: No daily data for the period.
    error_load: 'Unable to load analytics: {error}'
    quota_annotation: Quota ({limit:,}/day)
    chart_title: Volume per day
    chart_x: Date
    chart_y: Calls
  requests:
    error_load: 'Unable to load requests: {error}'
    pending_count: 🔔 {count} request(s) pending approval
    none_pending: No pending requests.
    subheader_pending: Pending requests
    select_request: Select a request
    detail_username: '**Username:** `{username}`'
    detail_email: '**Email:** {email}'
    detail_role: '**Requested role:** {role}'
    detail_message: '**Message:** {message}'
    btn_approve: ✅ Approve
    btn_reject: ❌ Reject
    reject_reason_label: Rejection reason (optional)
    approve_success: 'Account created for **{username}**. Send this token to {email}:'
    toast_rejected: Request rejected.
    error_approve: 'Error during approval: {error}'
    error_reject: 'Error during rejection: {error}'
    expander_history: 📜 Request history (approved / rejected)
    no_history: No history.
    status_approved: ✅ Approved
    status_rejected: ❌ Rejected
    col:
      id: ID
      id_help: Unique identifier of the access request.
      username: User
      username_help: Desired username from the person requesting access.
      username_hist_help: Requested username.
      email: Email
      email_help: Requester's email address. Used to send credentials.
      role_requested: Requested role
      role_requested_help: 'Desired access level: ''user'' (predictions), ''admin''
        (full management), ''readonly'' (view only).'
      role_hist_help: Access level requested at submission.
      message: Message
      message_help: Optional message left by the requester to explain their need.
      requested_at: Requested on
      requested_at_help: Date and time the request was submitted.
      status: Status
      status_help: '✅ Approved: account created. ❌ Rejected: request denied.'
      reviewed_at: Reviewed on
      reviewed_at_help: Date and time an administrator ruled on the request.
      rejection_reason: Rejection reason
      rejection_reason_help: Reason given by the administrator when rejecting. '—'
        if not provided.
code_example:
  page_title: Code Example — PredictML
  title: '💡 Code example: MLflow + API'
  intro: Complete example to train a model, track it with MLflow, then upload and
    use it via the API.
  tab_python: Python
  tab_curl: curl / bash
  tab_js: JavaScript
  footer_caption: 'API: `{api_url}` — MLflow: `{mlflow_url}`'
  mode_label: Code mode
  mode_minimal: Minimal
  mode_complete: Complete
  mode_help: "Minimal: quick start with essential calls only. Complete: production-ready\
    \ — all parameters from the reference scripts."
  python:
    section1_title: 1. Train a model and track it with MLflow
    section1_install: 'Install dependencies: `pip install scikit-learn mlflow boto3`'
    section2_title: 2. Upload the model via the API
    section3_title: 3. Make a prediction
    section4_title: 4. Record observed results (optional)
    section4_caption: Compare predictions against actual values.
  curl:
    section1_title: 1. Upload the model
    section2_title: 2. Make a prediction
    section3_title: 3. Retrieve prediction history
    section4_title: 4. Submit an observed result
  js:
    section1_title: 1. Upload the model
    section1_caption: _From a browser, the `.joblib` file must come from an `<input
      type="file">`._
    section2_title: 2. Make a prediction
    section3_title: 3. Retrieve prediction history
    section4_title: 4. Submit an observed result
retrain:
  page_title: Retrain — PredictML
  title: 🔄 Retraining Management
  caption: "Trigger manual retraining or schedule automatic crons per version. View logs and configure the auto-promotion policy."
  admin_only: ⛔ Access restricted to administrators.
  error_load_models: 'Unable to load models: {error}'
  no_models: No models available.
  filter_by_name: Filter by name
  search_placeholder: Search a model…
  tab_overview: 📅 Schedules
  tab_manual: 🚀 Manual Retrain
  tab_schedule: ⏰ Cron Schedule
  tab_policy: 🏆 Auto-promotion
  tab_history: 📜 History
  overview:
    subheader: Schedules Overview
    badge_active: 🟢 Active
    badge_disabled: 🔴 Disabled
    badge_none: ⚫ No schedule
    col_model: Model
    col_version: Version
    col_script: train.py Script
    col_cron: Cron
    col_lookback: Lookback (d)
    col_auto_promote: Auto-promote
    col_last_retrain: Last Retrain
    col_next_retrain: Next Retrain
    col_status: Status
    help_model: Name of the ML model associated with this retraining schedule.
    help_version: Version of the model on which the schedule is configured.
    help_script: ✅ A training script is available — automatic retraining is possible.
      ❌ No script — upload one via POST /models with train_file.
    help_cron: 'Cron expression (5 fields, UTC) defining the automatic trigger. E.g.:
      ''0 3 * * 1'' = every Monday at 3:00 UTC.'
    help_lookback: Number of history days used as training data (TRAIN_START_DATE = today - lookback).
    help_auto_promote: If enabled, the auto-promotion policy is evaluated after each scheduled retraining.
    help_last_retrain: Date and time of the last retraining triggered by this schedule.
    help_next_retrain: Scheduled date and time of the next automatic trigger.
    help_status: '🟢 Active: the schedule is enabled and will run automatically. 🔴
      Disabled: suspended. ⚫ No schedule: no schedule configured.'
    metric_active_schedules: Active schedules
    metric_trainable_models: Models with train.py
    metric_total_models: Total models
  manual:
    subheader: Trigger a Manual Retrain
    no_trainable: No model has a `train.py` script. Upload one via `POST /models`
      with the `train_file` parameter.
    select_model: Model to retrain
    start_date: Start date
    end_date: End date
    new_version_label: New version (leave empty = auto-generated)
    set_production: Set as production after training
    submit_btn: 🚀 Launch retraining
    error_date_order: Start date must be before end date.
    help_new_version: "Leave empty to auto-generate a timestamped version.\n\nExpected format: X.Y.Z or X.Y.Z-suffix\n• Digits and dots for the semantic version\n• Dashes and alphanumeric chars for the suffix\n\nValid examples:\n  1.3.0\n  1.3.0-retrain-20260621\n  2.0.0-custom\n\nForbidden characters: spaces, underscores, @, /, etc."
    error_version_format: 'Invalid format. Examples: 1.2.0 · 1.2.0-retrain-20260621 · 2.0.0-custom. Allowed characters: digits, dots, dashes and letters.'
    spinner: Retraining in progress… (may take up to 10 minutes)
    toast_success: 'Retraining successful! New version: {new_version}'
    auto_promoted_yes: ✅ Auto-promotion applied — the new version is in production.
    auto_promoted_no: '⚠️ Auto-promotion evaluated but not applied: {reason}'
    toast_failure: 'Retraining failed: {error}'
    unknown_error: Unknown error
    logs_stdout: 📋 Stdout logs
    logs_stderr: ⚠️ Stderr logs
    logs_empty: (empty)
    toast_error: 'Error during retraining: {error}'
  schedule:
    subheader: Configure a Cron Schedule
    help_expander: ℹ️ Help — cron expressions (UTC)
    help_table: '| Expression | Trigger |

      |---|---|

      | `0 3 * * 1` | Every Monday at 03:00 |

      | `0 2 * * *` | Every day at 02:00 |

      | `0 4 1 * *` | 1st of each month at 04:00 |

      | `0 6 * * 1,3,5` | Monday, Wednesday, Friday at 06:00 |


      Format: `minute hour day-of-month month day-of-week`

      '
    select_model: Model to schedule
    current_schedule: '**Current schedule:**'
    field_enabled_on: "✅ Active"
    field_enabled_off: "⬜ Paused"
    field_lookback_label: Lookback
    field_auto_promote_label: Auto-promotion
    field_last_run_label: Last run
    field_next_run_label: Next run
    field_yes: "✅ Yes"
    field_no: "⬜ No"
    cron_label: Cron expression (5 fields, UTC)
    cron_help: Leave empty to disable without clearing the expression
    lookback_label: History window lookback_days
    lookback_help: Number of days passed via TRAIN_START_DATE / TRAIN_END_DATE
    auto_promote_label: Auto-promotion after retrain
    auto_promote_help: Evaluates the promotion_policy after each scheduled run
    enabled_label: Enable schedule
    enabled_help: Uncheck to suspend without clearing the configuration
    save_btn: 💾 Save schedule
    toast_saved: Schedule saved for {name} v{version}.
    next_run_info: 'Next scheduled trigger: `{next_run}`'
    toast_error: 'Error saving schedule: {error}'
  policy:
    subheader: Post-retrain Auto-promotion Policy
    caption: The policy applies to all active versions of the selected model.
    model_label: Model
    current_policy: '**Current policy:**'
    no_policy: No auto-promotion policy defined for this model yet. Fill in the form
      below to create one.
    current_policy_title: "**Current policy for {name}:**"
    field_not_set: "—"
    field_auto_promote_label: Auto-promotion
    field_auto_promote_on: "✅ Active"
    field_auto_promote_off: "⬜ Inactive"
    field_min_accuracy_label: Min. accuracy
    field_min_auc_label: Min. AUC
    field_max_mae_label: Max. MAE
    field_max_latency_label: Max. P95 latency
    field_min_samples_label: Min. samples
    form_intro: "After each retraining, the new model version is automatically promoted
      to production **only if all activated criteria below are met**. Check a box
      to activate a criterion; uncheck it to ignore it."
    section_activation: Activation
    section_performance: Performance criteria
    section_speed: Speed criteria
    section_validation: Validation threshold
    label_auto_promote: Enable automatic promotion after each retraining
    label_min_accuracy: "Minimum accuracy required (0.0 → 1.0)"
    label_min_auc: "Minimum AUC-ROC (0.5 → 1.0)"
    label_max_mae: "Maximum MAE — mean absolute error (0.0 → 1.0)"
    label_max_latency: Maximum P95 response time (ms)
    label_min_samples: Minimum number of validation pairs required
    enable_min_accuracy: Activate the accuracy criterion
    help_min_accuracy: "The retrained model must reach at least this accuracy on (prediction,
      observed result) pairs. Example: 0.90 = 90% of predictions correct."
    enable_min_auc: "Activate the AUC criterion (binary classification only)"
    help_min_auc: "Area Under the ROC Curve — measures the model's ability to distinguish
      between two classes. 1.0 = perfect, 0.5 = random. Requires predicted probabilities
      to be stored. Example: 0.80."
    enable_max_latency: Activate the latency criterion
    help_max_latency: "95% of predictions must be served in under this time. Example:
      200 ms."
    enable_max_mae: Activate the MAE criterion (regression models only)
    help_max_mae: "For regression models: the mean absolute error must stay below
      this threshold (0.0 → 1.0 for normalized errors). Example: 0.05."
    help_min_samples: "Minimum number of (prediction, observed result) pairs needed
      to evaluate the policy. Below this count, auto-promotion is skipped even if
      all criteria would be met."
    enable_auto_promote: Enable auto-promotion
    help_auto_promote: "When enabled, the criteria above are evaluated automatically
      after each retraining. If all activated criteria are met, the new version is
      promoted to production without manual intervention."
    save_btn: 💾 Save policy
    toast_saved: Policy saved for {name} ({updated} version(s) updated).
    toast_error: 'Error saving policy: {error}'
  history:
    subheader: Retraining History
    model_label: Model
    error_load: 'Unable to load history: {error}'
    no_entries: No retraining recorded for this model. Retrains appear here after
      their first run.
    promo_auto: ✅ Auto
    promo_failed: ❌
    col_date: Date
    col_new_version: Created version
    col_trained_by: Trained by
    col_source_version: Source version
    col_accuracy: Accuracy
    col_auc: AUC-ROC
    col_f1: F1 Score
    col_auto_promotion: Auto-promotion
    col_reason: Reason
    col_train_start: Train start
    col_train_end: Train end
    help_date: Date and time when the retraining was triggered.
    help_new_version: Version number of the new model version created by this retraining.
    help_trained_by: 'Who triggered the retraining: a username or ''scheduler'' if
      automatic.'
    help_source_version: Version of the model used as a base before this retraining.
    help_accuracy: Accuracy of the retrained model on the test set. Between 0 and
      1 — closer to 1 is better.
    help_auc: AUC-ROC of the retrained model. 1.0 = perfect, 0.5 = random. Requires
      predicted probabilities (classification).
    help_f1: F1-score of the retrained model. Harmonic mean of precision/recall, robust
      to imbalanced classes.
    help_auto_promotion: '✅ Auto: the new version was automatically put into production
      per the configured policy. ❌: criteria not met. —: no policy or manual promotion.'
    help_reason: Explanation provided by the system if auto-promotion did not occur.
    help_n_rows: Number of training rows used during this retraining (if reported
      in train.py).
    help_train_start: Start date of the data window used for this retraining.
    help_train_end: End date of the data window used for this retraining.
    total_caption: '{total} retrain(s) total.'
    accuracy_chart_title: '#### Accuracy Progression'
    fi_delta_title: '#### Feature Importance Delta'
    fi_delta_caption: Compare SHAP feature importance before and after a retrain.
      🟠 Orange bars (>15%) and 🔴 red bars (>30%) indicate significant changes.
    fi_select_label: Retrain event to analyse
    fi_spinner: Loading feature importances…
    fi_error_baseline: 'Unable to load importance for source version ({version}):
      {error}'
    fi_error_new: 'Unable to load importance for new version ({version}): {error}'
    fi_no_data: No feature importance available for these versions (no predictions
      in the analysis window, or model not SHAP-compatible).
    fi_stability_metric: Importance stability
    fi_stability_help: '% of features that changed by less than 10% in absolute SHAP
      value'
    fi_sample_baseline: Sample v{version}
    fi_sample_new: Sample v{version}
    fi_trace_baseline: v{version} (baseline)
    fi_trace_new: v{version} (after retrain)
    fi_chart_title: Top-10 features — importance before/after retrain ({model})
    fi_xaxis: Feature
    fi_yaxis: Mean SHAP importance (|value|)
    fi_detail_expander: View per-feature details
    fi_col_feature: Feature
    fi_col_delta: Δ
    fi_col_delta_pct: Δ%
    fi_col_alert: Alert
    fi_help_feature: Name of the model input variable.
    fi_help_baseline_col: Mean SHAP importance (|value|) of this feature in version
      {version} before retraining.
    fi_help_new_col: Mean SHAP importance (|value|) of this feature in the new version
      {version} after retraining.
    fi_help_delta: SHAP importance difference between the new and old version (positive
      = feature became more important).
    fi_help_delta_pct: Percentage change in SHAP importance. > 30% = major change
      (🔴), > 15% = moderate (🟠), < 15% = stable (✅).
    fi_help_alert: Alert level based on importance change. 🔴 >30% = major, 🟠 >15%
      = moderate, ✅ <15% = stable.
aide:
  page_title: Help & AI Assistant — PredictML
  title: 💬 Help & AI Assistant
  subtitle: Ask your questions about PredictML — the assistant can **query the database**
    and **call the API** in real time to answer with your data.
  tools_available: 'Available tools: {tools}'
  doc_expander: 📚 Documentation
  doc_not_available: 'Documentation files are not accessible.


    In Docker mode, check that volumes are properly mounted in `docker-compose.yml`.'
  doc_select_label: Choose a document
  doc_enlarge_btn: ⛶ Enlarge
  doc_download_btn: ⬇ .md
  src_expander: 🔧 Source code ({count} files)
  src_file_label: File
  src_download_btn: ⬇ .py
  chat_expander: 💬 AI Assistant
  no_api_key: '**The `ANTHROPIC_API_KEY` key is not configured.**


    Add `ANTHROPIC_API_KEY=sk-ant-...` to the `.env` file then restart the Streamlit
    container.'
  quick_topics_expander: ⚡ Quick Topics
  new_conversation_btn: 🗑️ New conversation
  exchange_count: '{count} exchange(s)'
  chat_placeholder: Ask your question… (the assistant can query the DB and API)
  tool_running: Running…
  tool_summary_request: 'Executed request:'
  tool_summary_preview: 'Result preview:'
  quick_topics:
    prod_status: 🔎 Production model status
    prediction_stats: 📈 Prediction statistics
    generate_train_py: 🛠️ Generate a sklearn train.py
    api_calls: 🌐 Full Python API calls
    interpret_kpis: 📊 Interpret KPIs
    configure_ab: 🔬 Configure an A/B test
    auto_retrain: 🔄 Automatic cron retraining
    detect_drift: 🔍 Detect data drift
    manage_users: 👥 User management
    golden_tests: 🧪 Regression Golden Tests
    architecture: 🏗️ PredictML Architecture
    get_started: 🚀 Get started with PredictML

models:
  page_title: "Models — PredictML"
  title: "🤖 Model Management"
  caption: "Upload, compare and configure your ML models — metrics, feature importance, drift, regression tests and deployment actions."
  btn_refresh: "🔄 Refresh"
  load_error: "Unable to load models: {error}"
  no_models: "No models available."

  scripts_example:
    expander: "📋 Example Scripts"
    caption: "Reference scripts for uploading models and retraining. Download or preview them directly here."
    btn_view: "👁 Preview"
    btn_download: "⬇️ Download"
    dialog_title: "Script Preview"
    script_not_found: "# File not found: {path}"
    iris_train_desc: "RandomForest `train.py` script — upload to enable automatic retraining"
    iris_upload_desc: "Full RandomForest upload — trains locally and uploads via the API"
    wine_train_desc: "GradientBoostingRegressor `train.py` script — regression on alcohol content"
    wine_upload_desc: "Full Wine Regressor upload — trains locally and uploads via the API"

  upload:
    expander: "➕ Upload a New Model"
    section_files: "##### Files"
    pkl_label: "Model file (.joblib) *"
    pkl_help: "Serialized model file (joblib recommended). No size limit imposed."
    train_label: "Training script (train.py) — optional"
    train_help: "Must reference TRAIN_START_DATE, TRAIN_END_DATE, OUTPUT_MODEL_PATH."
    section_identity: "##### Identity"
    name_label: "Model name *"
    name_placeholder: "e.g. iris-classifier"
    version_label: "Version *"
    version_placeholder: "e.g. 1.0.0"
    description_label: "Description"
    description_placeholder: "Short description of the model…"
    algorithm_label: "Algorithm"
    section_metrics: "##### Metrics"
    accuracy_label: "Accuracy"
    auc_label: "AUC-ROC"
    auc_help: "AUC-ROC of the model on the test set (binary / multiclass classification)."
    f1_label: "F1 score"
    tags_label: "Tags (comma-separated)"
    tags_placeholder: "e.g. production, finance, v2"
    section_baseline: "##### Automatic Baseline"
    baseline_checkbox: "Compute baseline from production data"
    baseline_checkbox_help: "After upload, computes distribution statistics (mean, std, min, max) from recent predictions. Requires at least 100 predictions for this model name."
    baseline_days_label: "Window (days)"
    submit_btn: "⬆️ Upload Model"
    error_no_file: "Model file (.joblib) is required."
    error_no_name: "Model name is required."
    error_no_version: "Version is required."
    spinner: "Uploading {filename}…"
    progress_sending: "Sending file…"
    progress_in_progress: "Upload in progress…"
    progress_done: "Done."
    success_toast: "Model {name} v{version} uploaded."
    baseline_success: "Baseline computed: {n_features} features, {n_predictions} predictions used."
    baseline_warning: "Baseline not computed: {error}"
    upload_error: "Upload error: {detail}"

  summary:
    total_models: "Total models"
    in_production: "In production"
    with_mlflow: "With MLflow"
    caption: "Accuracy (eval), F1 (eval), R² (eval), RMSE (eval): metrics from test set evaluation at training time."

  filters:
    search_label: "Name or description"
    search_placeholder: "Search…"
    model_label: "Model"
    model_all: "(all models)"
    tag_label: "Tag"
    tag_all: "(all tags)"

  table:
    col_name: "Name"
    col_name_help: "Unique model identifier."
    col_version: "Version"
    col_version_help: "Model version in X.Y.Z format."
    col_tags: "Tags"
    col_tags_help: "Free labels associated with the model (e.g. Example, production, v2…)."
    col_algorithm: "Algorithm"
    col_algorithm_help: "scikit-learn class used during training (e.g. RandomForest, GradientBoosting…)."
    col_task: "Task"
    col_task_help: "ML task type: 📈 Regression, 🔵 Binary classification or 🟡 Multiclass classification."
    col_baseline: "Baseline"
    col_baseline_help: "✅ Baseline: reference statistics (mean, std, min/max) computed at training. Used for drift detection and input schema validation. ⚠️ Missing: uploaded without feature_baseline."
    col_cache: "Cache"
    col_cache_help: "🔥 Cached: model loaded in memory — ready to serve without cold-start latency. ❄️ Not loaded: will be loaded from MinIO on the first prediction."
    col_status: "Status"
    col_status_help: "🟢 Production: main version. 🟠 A/B (x%): receives x% of traffic in A/B test. 🟣 Shadow: receives requests without returning prediction to client. ✅ Active: available but not in production. ⚫ Inactive: disabled."
    col_creator: "Creator"
    col_creator_help: "User who uploaded this model."
    col_created_at: "Created at"
    col_created_at_help: "Model upload date (local time)."
    col_last_pred: "Last pred."
    col_last_pred_help: "Date and time of the last prediction served by this model."
    col_accuracy_eval: "Accuracy (eval)"
    col_accuracy_eval_help: "Accuracy = (true positives + true negatives) / total observations.\n\nMeasures the proportion of correct predictions on the hold-out test set.\n\n• 1.0 → 100% correct predictions\n• 0.5 → equivalent to random on 2 classes\n\n⚠️ Misleading on highly imbalanced classes. Classification metric only."
    col_auc_eval: "AUC (eval)"
    col_f1_eval: "F1 (eval)"
    col_f1_eval_help: "F1 = 2 × (Precision × Recall) / (Precision + Recall)\n\nHarmonic mean of precision and recall. Recommended for imbalanced classes. Classification metric only."
    col_r2_eval: "R² (eval)"
    col_r2_eval_help: "R² = 1 − (Σ(y − ŷ)²) / (Σ(y − ȳ)²)\n\nMeasures the proportion of variance in the target explained by the model, computed on the held-out test set (eval). Regression metric only."
    col_rmse_eval: "RMSE (eval)"
    col_rmse_eval_help: "RMSE = √( Σ(y − ŷ)² / n )\n\nRoot mean squared error, expressed in the same unit as the target variable. Computed on the held-out test set (eval). Regression metric only."
    baseline_ok: "✅ Baseline"
    baseline_missing: "⚠️ No baseline"
    cache_hot: "🔥 Cached"
    cache_cold: "❄️ Not loaded"
    status_production: "🟢 Production"
    status_active: "✅ Active"
    status_inactive: "⚫ Inactive"
    task_regression: "📈 Regression"
    task_binary: "🔵 Binary"
    task_multiclass: "🟡 Multiclass"

  compare:
    expander: "📊 Multi-version Comparison"
    filter_label: "Filter by name"
    filter_placeholder: "Search for a model…"
    model_label: "Model to compare"
    versions_label: "Versions to include (empty = all)"
    date_start: "Start date"
    date_end: "End date"
    btn_compare: "🔍 Compare"
    date_order_warning: "Start date must be before end date."
    spinner: "Comparing…"
    no_versions: "No active versions found."
    col_version: "Version"
    col_task: "Task"
    col_nb_pred: "Prod. predictions"
    col_nb_pred_help: "Production predictions (is_shadow=False) over the period {start} → {end}."
    col_nb_shadow: "Shadow pred."
    col_nb_shadow_help: "Shadow predictions (is_shadow=True) over the period {start} → {end}."
    col_lat_p50: "Latency p50 (ms)"
    col_lat_p95: "Latency p95 (ms)"
    col_trained_at: "Trained at"
    col_drift: "Drift"
    col_mae_eval: "MAE (eval)"
    col_mae_eval_help: "MAE on the training test set."
    col_mae_live: "MAE (live)"
    col_mae_live_help: "MAE computed on real predictions with ground truth."
    col_rmse_eval: "RMSE (eval)"
    col_rmse_eval_help: "RMSE on the training test set."
    col_rmse_live: "RMSE (live)"
    col_rmse_live_help: "RMSE computed on real predictions with ground truth."
    col_r2_eval: "R² (eval)"
    col_r2_eval_help: "R² on the training test set."
    col_r2_live: "R² (live)"
    col_r2_live_help: "R² computed on real predictions with ground truth."
    col_accuracy_eval: "Accuracy (eval)"
    col_accuracy_eval_help: "Accuracy on the training test set."
    col_accuracy_live: "Accuracy (live)"
    col_accuracy_live_help: "Accuracy on real predictions with ground truth."
    col_auc_eval: "AUC (eval)"
    col_auc_eval_help: "AUC-ROC on the training test set. Requires predicted probabilities."
    col_auc_live: "AUC (live)"
    col_auc_live_help: "AUC-ROC computed on real predictions with ground truth and probabilities."
    col_f1_eval: "F1 (eval)"
    col_f1_eval_help: "F1 on the training test set."
    col_f1_live: "F1 (live)"
    col_f1_live_help: "F1 on real predictions with ground truth."
    col_brier: "Brier score"
    col_brier_help: "Squared error between predicted probability and actual outcome (0 = perfect, 1 = worst).\n\n• < 0.10 → very good\n• 0.10–0.25 → acceptable\n• > 0.25 → needs improvement\n\nClassifiers with probabilities, ≥ 30 ground truth pairs."
    caption: "Compared on {compared_at} UTC — period: {start} → {end}"
    roc_title: "#### 📉 ROC Curve — Multi-version Comparison"
    roc_caption: "Computed from real predictions with ground truth (period: {start} → {end}). Requires predicted probabilities and observed_results."
    roc_random: "Random (AUC = 0.50)"
    roc_unavailable: "📉 ROC curve unavailable — requires predicted probabilities and corresponding observed_results for at least one version in this period."
    compare_error: "Comparison error: {error}"
    col_metric_eval_help: "{col} on the training test set."
    col_metric_live_help: "{col} computed on real predictions with ground truth."

  detail:
    subheader: "Detail & Actions"
    filter_label: "Filter by name"
    filter_placeholder: "Search for a model…"
    select_label: "Select a model"

  details_expander: "📋 Full Details"
  detail_name: "**Name:** `{name}`"
  detail_version: "**Version:** `{version}`"
  detail_description: "**Description:** {value}"
  detail_algorithm: "**Algorithm:** {value}"
  detail_task: "**Task type:** {value}"
  detail_training_dataset: "**Training dataset:** `{value}`"
  detail_trained_by: "**Trained by:** {value}"
  detail_derived_from: "**Derived from:** `v{version}`"
  detail_tags: "**Tags:** {tags}"
  detail_tags_empty: "**Tags:** —"
  detail_webhook: "**Webhook URL:** `{url}`"
  detail_webhook_empty: "**Webhook URL:** —"
  detail_webhook_help: "Receives a JSON POST on every alert triggered for this model
    (error rate, drift, AUC). Configure the URL via ⚙️ Edit metadata or via
    PATCH /models/{name}/{version} with {\"webhook_url\": \"https://…\"}.
    Email and webhook are independent: the webhook fires even when email alerts
    are disabled."
  detail_mlflow: "**MLflow run:** [{run_id}]({link})"
  detail_mlflow_empty: "**MLflow run:** —"
  detail_minio: "**MinIO object:** `{bucket}/{minio_key}`"
  detail_file_size: "**File size:** {size} KB"
  detail_nb_features: "**# features:** {value}"
  detail_last_pred: "**Last prediction:** {value}"
  detail_classes: "**Classes:** {value}"
  detail_confidence_threshold: "**Confidence threshold:** {value}"
  detail_confidence_threshold_help: "Minimum confidence score (0–1) below which a prediction is flagged as uncertain. If the model's top-class probability is below this threshold, the prediction is marked with a warning in the response. Set via ⚙️ Edit metadata."
  detail_hyperparams: "**Hyperparameters:**"
  detail_hyperparams_empty: "**Hyperparameters:** —"
  detail_param_col: "Parameter"
  detail_value_col: "Value"
  badge_ab_test: "🟠 A/B test active — traffic {weight}"
  badge_ab_test_no_weight: "🟠 A/B test active"
  badge_shadow: "🟣 Shadow active"
  badge_production: "🟢 In production"
  badge_active: "✅ Active (not production)"
  badge_inactive: "⚫ Inactive"
  btn_download_dataset: "⬇ Download training dataset"
  btn_download_script: "⬇ Download training script"
  btn_hide_script: "🙈 Hide script"
  btn_view_script: "👁 Preview script"
  btn_download_model: "⬇️ Download model{size}"

  feature_importances:
    expander: "📊 Feature Importances"
    source_note: "Importances extracted at training time from the sklearn model's `feature_importances_` attribute (tree-based models). These values are static and do not reflect the model's behaviour in production."
    chart_header: "**Relative feature importance**"
    table_header: "**Ranking**"
    axis_importance: "Importance (%)"
    axis_feature: Feature
    col_feature: Feature
    col_importance: "Importance (%)"
    caption: "{n} features — normalized scores (sum = 100 %)"
  analysis:
    expander: "📊 Analysis & Monitoring"
    date_start: "Start date"
    date_end: "End date"
    btn_load: "📊 Load analysis"
    btn_load_caption: "Loads SHAP metrics, performance, output drift and feature drift."
    volume_header: "#### 📦 Volume"
    volume_prod_count: "Prod predictions"
    volume_shadow_count: "Shadow predictions"
    volume_first_date: "First prediction"
    volume_last_date: "Last prediction"
    volume_gt_pct: "With ground truth"
    volume_gt_pct_help: "{n} prediction(s) out of {total} matched with an observed result."
    volume_error: "Unable to load volume stats: {error}"
    metrics_header: "#### 📈 Metrics"
    col_metric: "Metric"
    col_training: "Training"
    col_production: "Production"
    col_delta: "Δ"
    col_training_help: "Metrics evaluated on the test set during initial training."
    col_production_help: "Metrics computed on predictions matched with observed results from {start} to {end}.\n\n{n} matched prediction(s). Fed via `POST /observed-results`."
    col_delta_help: "Gap Production − Training.\n\n- **Negative**: degradation in production\n- **Positive**: improvement (or different distribution)\n- **—**: insufficient data"
    no_ground_truth: "No ground truth data — send results via `POST /observed-results` to see the Production column."
    export_model_card_btn: "📄 Export model card"
    model_card_error: "Model card unavailable: {error}"
    shap_header: "#### 📊 Feature Importance (SHAP)"
    shap_slider_label: "SHAP sample ({total} predictions available in period)"
    shap_slider_help: "Number of recent predictions used to compute SHAP importance.\n\n**SHAP** estimates each feature's contribution to each prediction — computationally expensive, hence this limit.\n\n- **Small**: fast, less representative\n- **Medium**: good speed/accuracy tradeoff ✅\n- **Large**: more accurate, slower\n\nPredictions are selected in **reverse chronological order** (the N most recent in the selected period, `ORDER BY id DESC`)."
    shap_no_data: "Not enough predictions yet to compute feature importance."
    shap_chart_title: "Top {n} features — {model} v{version}"
    shap_xaxis: "SHAP importance (mean absolute value)"
    shap_caption: "{n} predictions analyzed — from {start} to {end} — version: {version}"
    shap_error_422: "This model does not support SHAP feature importance (train with a pandas DataFrame to enable this feature)."
    shap_error_404: "Model not found."
    shap_error_generic: "Unable to compute feature importance: {error}"
    perf_header: "#### 📈 Performance Metrics"
    perf_caption: "Computed on production predictions matched with an observed result (via `POST /observed-results`). Does not reflect predictions without ground truth."
    perf_no_data: "No (prediction, observed result) pair available. Add results via POST /observed-results to activate this view."
    perf_n_paired: "{n} matched predictions"
    perf_error: "Unable to load performance metrics: {error}"
    confusion_title: "Confusion matrix"
    confusion_predicted: "Predicted"
    confusion_actual: "Actual"
    confusion_count: "Count"
    per_class_header: "**Per-class metrics**"
    col_class: "Class"
    col_class_help: "Predicted class. The number in parentheses is the numeric index stored in the database."
    col_precision: "Precision"
    col_precision_help: "**Precision**: among all predictions for this class, what fraction was correct.\n\n`TP / (TP + FP)`\n\nHigh = few false positives for this class."
    col_recall: "Recall"
    col_recall_help: "**Recall (sensitivity)**: among all true examples of this class, what fraction was correctly detected.\n\n`TP / (TP + FN)`\n\nHigh = few false negatives for this class."
    col_f1: "F1"
    col_f1_help: "**F1-score**: harmonic mean of Precision and Recall.\n\n`2 × (Precision × Recall) / (Precision + Recall)`\n\nGood tradeoff when classes are imbalanced."
    col_support: "Support"
    col_support_help: "Number of predictions matched to an observed result for this class over the selected period.\n\nLow support makes metrics statistically less reliable."
    output_drift_header: "#### 📊 Output Drift (label shift)"
    output_drift_no_baseline: "No training label distribution available. Make sure the `train.py` script prints a JSON with `label_distribution`."
    output_drift_insufficient: "Insufficient data ({n} predictions from {start} to {end} — minimum: 30)."
    output_drift_psi: "PSI"
    output_drift_psi_help: "Population Stability Index: ok < 0.1 | warning 0.1–0.2 | critical ≥ 0.2"
    output_drift_n_analyzed: "Predictions analyzed"
    output_drift_by_class_header: "**Distribution by class**"
    output_drift_col_class: "Class"
    output_drift_col_class_help: "Class predicted by the model. The number in parentheses is the numeric index stored in the database."
    output_drift_col_baseline: "Baseline"
    output_drift_col_baseline_help: "Proportion of this class in the **training dataset** (stored in `training_stats.label_distribution`).\n\nSource: `train.py` script → JSON stdout → key `label_distribution`."
    output_drift_col_current: "Current"
    output_drift_col_current_help: "Proportion of this class in **production predictions** over the selected period.\n\nComputed via `GROUP BY prediction_result` on the `predictions` table."
    output_drift_col_delta: "Δ"
    output_drift_col_delta_help: "Gap between current proportion and baseline (Current − Baseline).\n\n- **Positive**: overrepresentation of this class in production\n- **Negative**: underrepresentation\n\nA high Δ contributes to a critical PSI."
    output_drift_chart_title: "Baseline vs current distribution"
    output_drift_status: "**Status:** {status}"
    output_drift_od_no_baseline: "⬜ no baseline"
    output_drift_od_insufficient: "⬜ insufficient data"
    output_drift_error: "Unable to compute output drift: {error}"
    feature_drift_header: "#### 🌡️ Feature Drift (inputs)"
    feature_drift_global_status: "**Global status:** {status}"
    feature_drift_n_analyzed: "{n} predictions analyzed from {start} to {end}"
    feature_drift_no_baseline: "No feature baseline registered for this model. Make sure the `train.py` script prints a JSON with `feature_stats`."
    feature_drift_no_data: "No feature drift data available for this period."
    feature_drift_col_feature: "Feature"
    feature_drift_col_status: "Status"
    feature_drift_col_status_help: "**Drift status** of the feature, computed from Z-score and PSI.\n\n- 🟢 **ok**: no significant drift detected\n- 🟡 **warning**: moderate drift — monitor\n- 🔴 **critical**: significant drift — retraining recommended\n- ⬜ **no_data**: not enough predictions to compute drift"
    feature_drift_col_prod_mean: "Prod. mean"
    feature_drift_col_prod_mean_help: "Mean of the feature on production predictions over the selected period."
    feature_drift_col_baseline_mean: "Baseline mean"
    feature_drift_col_baseline_mean_help: "Mean of the feature in the training dataset (stored in `feature_baseline`)."
    feature_drift_col_zscore: "Z-score"
    feature_drift_col_zscore_help: "**Z-score**: gap between production mean and baseline mean, normalized by baseline standard deviation.\n\n- 🟢 **ok**: Z < 2σ\n- 🟡 **warning**: 2σ ≤ Z < 3σ\n- 🔴 **critical**: Z ≥ 3σ\n\nA high Z-score indicates the feature distribution in production has significantly drifted from training data."
    feature_drift_col_psi: "PSI"
    feature_drift_col_psi_help: "**PSI (Population Stability Index)**: measures the global distribution change of a feature between baseline and production.\n\n- 🟢 **ok**: PSI < 0.1 — stable distribution\n- 🟡 **warning**: 0.1 ≤ PSI < 0.2 — moderate change\n- 🔴 **critical**: PSI ≥ 0.2 — major change, retraining recommended\n\nComputed on the **last 5,000 predictions** in the selected period via Gaussian bin approximation.\n\n— = unavailable (requires a `feature_baseline` registered on the model)."
    feature_drift_col_n_prod: "N prod."
    feature_drift_col_n_prod_help: "Number of production predictions used to compute drift over the selected period. Low N makes drift less statistically reliable."
    feature_drift_zscore_chart_title: "Z-score per feature (|Δμ / σ baseline|)"
    feature_drift_zscore_chart_help: "Gap between production mean and baseline mean, normalized by baseline std dev. The higher the value, the more the feature has drifted. Thresholds: ok < 2 | warning 2–3 | critical ≥ 3. Bars sorted by absolute value ascending."
    feature_drift_psi_chart_title: "PSI per feature (Population Stability Index)"
    feature_drift_psi_chart_help: "Population Stability Index: measures the overall distribution shift between the training dataset (baseline) and production predictions. Thresholds: ok < 0.1 | warning 0.1–0.2 | critical ≥ 0.2. Computed on the last 5,000 predictions in the period."
    feature_drift_warning_2sigma: "warning threshold (2σ)"
    feature_drift_critical_3sigma: "critical threshold (3σ)"
    feature_drift_warning_01: "warning threshold (0.1)"
    feature_drift_critical_02: "critical threshold (0.2)"
    feature_drift_error: "Unable to load feature drift: {error}"

  validate:
    expander: "🔍 Validate JSON Schema"
    intro: "Test a JSON payload before sending a prediction to detect missing, unexpected or wrongly typed features."
    json_label: "JSON payload to validate"
    btn_validate: "✅ Validate"
    json_invalid: "Invalid JSON: {error}"
    spinner: "Validating…"
    valid: "✅ Schema valid"
    invalid: "❌ Schema invalid"
    missing_feature: "❌ **Missing feature**: `{feature}`"
    unexpected_feature: "❌ **Unexpected feature**: `{feature}`"
    type_coercion: "⚠️ **Type coercion**: `{feature}` ({from_type} → {to_type})"
    expected_features_header: "**Expected features:**"
    validate_error: "Validation error: {error}"
    code_example_header: "**💻 Python example — `/predict` call**"
    response_example_header: "**Response example:**"

  golden_tests:
    expander: "🧪 Regression Tests"
    intro: "**What is it for?**  \nBuild a reference test set (features → expected output) to detect **regressions** after a retrain or model update: if a case that previously passed now fails, the model has regressed.\n\n**How does it work?**\n1. **Add** test cases: input features + expected prediction + optional description.\n2. **Run** the tests (▶️): each case is submitted live to the selected model.\n3. **Result**: ✅ if the prediction matches the expected output, ❌ otherwise.\n\n*Tip: cover critical business cases, boundary values and representative examples for each class.*"
    n_cases: "**{n} registered case(s)**"
    no_cases: "No golden test cases registered for this model."
    input_features_header: "**Input features**"
    expected_output_header: "**Expected output**"
    btn_delete: "🗑 Delete"
    delete_toast: "Test #{id} deleted."
    delete_error: "Error: {error}"
    add_expander: "➕ Add a test case"
    features_label: "Features (JSON)"
    expected_label: "Expected output"
    expected_placeholder: "setosa"
    description_label: "Description (optional)"
    submit_btn: "Add"
    error_no_expected: "Expected output is required."
    add_success: "Test case added."
    add_error: "Error: {error}"
    csv_expander: "📥 Import from CSV"
    csv_caption: "Format: feature columns + `expected_output` (required) + `description` (optional). Example: `sepal_length,sepal_width,petal_length,petal_width,expected_output,description`"
    csv_uploader_label: "Choose a CSV file"
    csv_import_btn: "Import"
    csv_import_success: "{n} cases imported."
    csv_import_errors: "Errors: {errors}"
    csv_import_error: "Import error: {error}"
    run_btn: "▶️ Run tests"
    col_passed: "✅ Passed"
    col_failed: "❌ Failed"
    col_rate: "Rate"
    no_test_cases: "No test cases registered."
    result_expected_header: "**Expected**"
    result_actual_header: "**Actual**"
    result_features_header: "**Features used**"
    run_error: "Error running tests: {error}"
    full_page_link: "🧪 View Golden Tests page →"

  whatif:
    expander: "🔮 What-if Explorer"
    no_baseline: "⚠️ This model has no feature baseline. Compute one in **Admin Actions → Compute baseline** to enable the What-if explorer."
    caption: "{n} features — adjust the sliders then click **Predict**."
    shap_checkbox: "Show SHAP contributions"
    btn_predict: "🔮 Predict"
    spinner: "Predicting…"
    metric_prediction: "Prediction"
    metric_max_prob: "Max probability"
    low_confidence: "⚠️ Low confidence"
    class_probs_header: "**Class probabilities:**"
    col_class: "Class"
    col_class_i: "Class {i}"
    col_probability: "Probability"
    shap_header: "**SHAP contributions:**"
    shap_positive: "Positive"
    shap_negative: "Negative"
    shap_unavailable: "SHAP values are not available for this model type."
    predict_error: "Prediction error: {error}"
    history_header: "**History of tested combinations**"
    feature_select_label: "Feature to analyze"
    evolution_title: "Evolution vs {feature}"
    y_label_prob: "Max probability"
    y_label_pred: "Prediction"
    btn_clear_history: "🗑️ Clear history"

  admin:
    expander: "⚙️ Admin Actions"
    readiness_header: "**Production-readiness checklist**"
    check_file_accessible: "File accessible in MinIO"
    check_baseline_computed: "Feature baseline computed"
    check_no_critical_drift: "No critical drift (24h)"
    check_is_production: "Marked as production"
    btn_promote: "🚀 Set as production"
    btn_promote_help: "Resolve the ❌ checks above before promoting"
    already_production: "🟢 Already in production"
    promote_toast: "{name} v{version} is now in production."
    promote_error: "Error: {error}"
    cache_hot: "🔥 Cached"
    cache_cold: "❄️ Not loaded"
    btn_warmup: "🔥 Warm up cache"
    btn_warmup_help: "Load the model into memory to eliminate cold-start latency"
    warmup_spinner: "Loading model into cache…"
    warmup_toast: "Model loaded in {ms:.0f} ms — cache key: {key}"
    warmup_error: "Warmup error: {error}"
    cache_already_loaded: "Model already cached — no warmup needed."
    btn_delete: "🗑️ Delete this version"
    confirm_delete: "Delete **{name} v{version}**? (MinIO file + MLflow run)"
    confirm_yes: "Yes, delete"
    confirm_cancel: "Cancel"
    delete_toast: "Model deleted."
    delete_error: "Error: {error}"

  edit_meta:
    expander: "✏️ Edit Metadata"
    section_description: "**Description & information**"
    description_label: "Description"
    description_placeholder: "Model description…"
    dataset_label: "Training dataset"
    dataset_placeholder: "e.g. iris_v2, s3://bucket/data.csv"
    dataset_help: "Free reference to the dataset used to train this model."
    section_tags: "**Tags & Webhook**"
    tags_label: "Tags (comma-separated)"
    tags_placeholder: "production, finance, v2"
    webhook_label: "Webhook URL"
    webhook_placeholder: "https://example.com/webhook"
    webhook_help: "URL called by the alert worker on every critical event (error
      spike, drift, low AUC). The worker sends a JSON POST with the model name,
      version, timestamp, and alert details. Works independently of email alerts."
    section_inference: "**Inference**"
    confidence_label: "Confidence threshold"
    confidence_help: "Threshold below which the prediction is marked `low_confidence=True` in the response."
    section_deployment: "**A/B / Shadow Deployment**"
    deploy_mode_label: "Deployment mode"
    deploy_mode_help: "production = direct traffic · ab_test = weighted routing · shadow = silent execution"
    deploy_production: "🟢 Production"
    deploy_ab_test: "🟠 A/B Test"
    deploy_shadow: "🟣 Shadow"
    traffic_weight_label: "Traffic weight (0.0 – 1.0)"
    traffic_weight_help: "Fraction of traffic routed to this version (e.g. 0.3 = 30%)"
    section_alerts: "**Alert thresholds**"
    alerts_caption: "Set to 0 to disable the corresponding threshold."
    accuracy_min_label: "Min accuracy"
    accuracy_min_help: "Alert if live accuracy drops below this threshold. 0 = disabled."
    error_rate_max_label: "Max error rate"
    error_rate_max_help: "Alert if error rate exceeds this threshold. 0 = disabled."
    drift_auto_alert_label: "Auto drift alert"
    drift_auto_alert_help: "Enable automatic alerts when critical drift is detected."
    btn_save: "💾 Save"
    save_toast: "Metadata updated."
    save_error: "Error: {error}"
    no_change: "No changes detected."

  retrain:
    expander: "🔄 Retrain"
    title: "**Retrain** `{name}` v`{version}`"
    start_date: "Start date"
    end_date: "End date"
    new_version_label: "New version (leave empty = auto-generated)"
    set_prod_label: "Set as production after training"
    submit_btn: "🚀 Launch retraining"
    date_order_error: "Start date must be before end date."
    spinner: "Retraining in progress… (may take up to 10 minutes)"
    success_toast: "Retraining successful! New version: {new_version}"
    failure_toast: "Retraining failed: {error}"
    unknown_error: "Unknown error"
    stdout_expander: "📋 stdout logs"
    stderr_expander: "⚠️ stderr logs"
    logs_empty: "(empty)"
    error_toast: "Retraining error: {error}"

  policy:
    expander_active: "⚙️ Promotion policy — 🚀 Promo ACTIVE  ⚡ CB ACTIVE"
    expander_promote_only: "⚙️ Promotion policy — 🚀 Promo ACTIVE  ⬜ CB inactive"
    expander_cb_only: "⚙️ Promotion policy — ⬜ Promo inactive  ⚡ CB ACTIVE"
    expander_default: "⚙️ Promotion policy — ⬜ all inactive"
    caption: "Applied to **all versions** of `{name}`. 0 = criterion disabled."
    intro: "Automates two lifecycle mechanisms for this model:\n\n🚀 **Auto-promotion** — automatically promotes a new version to production if quality criteria are met after a retrain.\n\n⚡ **Circuit breaker** — automatically demotes the production version if its performance degrades during operation."
    status_box_both_active: "🟢 Both **Auto-promotion** and **Circuit breaker** are **active** for this model."
    status_box_promote_only: "🚀 **Auto-promotion** is **active**. ⬜ Circuit breaker **inactive**."
    status_box_cb_only: "⚡ **Circuit breaker** is **active**. ⬜ Auto-promotion **inactive**."
    status_box_none: "⬜ No automation active. Promotions and demotions remain manual."
    howto_expander: "💡 How does it work?"
    howto_callout: "**How does it work?**\n\n🚀 **Auto-promotion**: after each retrain, if all criteria (accuracy, MAE, latency…) are met, the new version is automatically set to production.\n\n⚡ **Circuit breaker**: every **6 hours**, the `retrain-worker` container monitors the production model. If it detects critical drift or accuracy dropping below the threshold, it automatically removes the model from production (`is_production=false`)."
    promote_header_active: "**🚀 Auto-promotion after retrain** — 🟢 Enabled"
    promote_header_inactive: "**🚀 Auto-promotion after retrain** — ⬜ Inactive"
    promote_help: "After each retrain, the new version is evaluated on recent (prediction → observed result) pairs. If **all** configured criteria are met and the minimum sample count is reached, it is automatically set to production. Setting 0 on a criterion disables it."
    auto_promote_label: "Enable auto-promotion"
    min_accuracy_label: "Minimum accuracy (0 = disabled)"
    min_accuracy_help: "Accuracy required on recent (prediction → observed result) pairs to approve promotion. Example: 0.90 → the new version must predict correctly at least 90 % of validation cases."
    min_golden_label: "Min Golden Test pass rate (0 = disabled)"
    min_golden_help: "Proportion of golden tests that must pass. Example: 0.95 → 95 % of reference test cases must match. Configure golden tests in the Golden Tests page."
    max_mae_label: "Maximum MAE (0 = disabled)"
    max_mae_help: "Maximum allowed Mean Absolute Error (regression models only). Example: 5.0 → average prediction error must not exceed 5 units."
    max_latency_label: "Max P95 latency (ms, 0 = disabled)"
    max_latency_help: "The 95th-percentile response time of the new version must stay below this threshold. Example: 200 → 95 % of predictions must complete in under 200 ms."
    min_samples_label: "Min validation pairs"
    min_samples_help: "Minimum number of (prediction, observed result) pairs required for a reliable evaluation. If not enough ground-truth data is available, auto-promotion is skipped."
    criteria_summary_header: "**✅ Active promotion criteria (all must be satisfied):**"
    criteria_accuracy: "Accuracy ≥ {val:.2f}"
    criteria_mae: "MAE ≤ {val}"
    criteria_latency: "P95 latency ≤ {val:.0f} ms"
    criteria_samples: "At least {val} validation pairs"
    criteria_golden: "Golden Tests pass rate ≥ {val:.2f}"
    criteria_none: "⚠️ Auto-promotion is **enabled but no quality criterion is configured**. The new version will be promoted automatically after any retrain, without any quality check."
    cb_header_active: "**⚡ Circuit breaker (auto-demotion)** — 🔴 Enabled"
    cb_header_inactive: "**⚡ Circuit breaker (auto-demotion)** — ⬜ Inactive"
    cb_help: "Continuously monitors the production version **every 6 hours** (via the `retrain-worker` container). If feature drift exceeds the configured level **or** accuracy drops below the threshold, the version is automatically demoted (`is_production=false`)."
    cb_frequency_note: "🕐 Checked automatically every **6 hours** by the `retrain-worker` container."
    auto_demote_label: "Enable circuit breaker"
    drift_level_label: "Drift trigger level"
    drift_warning: "⚠️ Warning (more sensitive)"
    drift_critical: "🔴 Critical (strict — recommended)"
    drift_level_help: "`Warning` = triggers on moderate drift. `Critical` = triggers only on severe drift. Use `Critical` to avoid unnecessary demotions."
    demote_accuracy_label: "Min accuracy before demotion (0 = disabled)"
    demote_accuracy_help: "If the production model's accuracy on recent pairs drops below this threshold, it is automatically demoted. Example: 0.80 → demotion if accuracy falls below 80 %."
    cooldown_label: "Cooldown (hours)"
    cooldown_help: "Minimum time between two circuit breaker triggers. Avoids oscillations (promotion → demotion → promotion…). Set to 0 to disable the cooldown."
    btn_save: "💾 Save policy"
    save_toast: "Policy updated."
    save_error: "Error: {error}"

  baseline:
    expander: "📐 Compute baseline from production"
    description: "Computes `{mean, std, min, max}` per feature from recent production predictions and saves the result as the model's **feature_baseline**, enabling drift detection."
    existing_warning: "⚠️ A baseline already exists for this model. Recomputing will overwrite the current values and reset drift detection."
    days_slider_label: "Time window (days)"
    dry_run_label: "dry_run (simulate without saving)"
    btn_compute: "Compute"
    spinner: "Computing…"
    result_caption: "**Predictions used:** {n} — window: {days} days"
    dry_run_info: "Uncheck **dry_run** to save the baseline."
    save_toast: "Baseline saved — drift active for this model."
    error: "Error: {error}"

  history:
    expander: "📜 Change History"
    load_error: "Unable to load history: {error}"
    no_history: "No history available for this version."
    caption: "{total} entry/entries total — showing the last 20"
    entry_line: "**{ts}** — {badge} — by **{who}**  \nChanged fields: `{changed}`"
    changed_by_unknown: "unknown"
    btn_rollback: "↩ Rollback"
    rollback_help: "Restore state from entry #{id}"
    snapshot_expander: "Snapshot #{id}"
    confirm_rollback: "Restore metadata of **{model} v{version}** to the state captured in entry **#{id}**?  \nThis action is irreversible (but logged in history)."
    confirm_yes: "✅ Yes, restore"
    confirm_cancel: "❌ Cancel"
    rollback_toast: "Rollback done — entry #{id} created."
    rollback_error: "Rollback error: {error}"

autotrain:
  page_title: AutoTrain — PredictML
  title: 🤖 AutoTrain ML Coach
  subtitle: Generate, test and upload train.py scripts with AI assistance.
  tab_chat: 💬 Chat
  tab_data: 📊 Data
  tab_scripts: 📝 Generated Scripts
  no_api_key: ANTHROPIC_API_KEY not configured — chatbot is disabled.
  new_conversation_btn: 🗑️ New Conversation
  chat_placeholder: Describe the model you want to create…
  tool_running: Running…
  tool_summary_request: Request
  tool_summary_preview: Result preview
  dataset_badge: "✅ Dataset: {n_rows} rows ({n_labeled} labeled)"
  model_ready_badge: "✅ Model ready to upload"
  exchange_count: "{count} exchange(s)"
  quick_topics:
    analyze_data: 🔍 Analyze Data
    random_forest: 🌲 Generate RandomForest
    gradient_boosting: 📈 GradientBoosting
    compare_versions: ⚖️ Compare Scripts
    optimize_script: 🔧 Optimize Script
    upload_model: 🚀 Upload Model
  data:
    source_label: Data source
    source_upload: 📂 Upload a local CSV
    source_api: 🌐 Fetch from API
    upload_label: Training CSV file
    upload_help: "Expected columns: id_obs, input_features, prediction_result, observed_result,
      timestamp, model_version, response_time_ms"
    model_label: Source model
    date_start_label: Start date
    date_end_label: End date
    limit_label: Row limit
    fetch_btn: 🔄 Load data
    fetch_spinner: Fetching data from API…
    fetch_success: "Data loaded: {n_rows} rows ({n_labeled} labeled)"
    fetch_error: "Error fetching data: {error}"
    no_models: No models available.
    no_data: No dataset loaded. Use the Data tab.
    n_rows: Total rows
    n_labeled: Labeled
    n_features: Features
    preview_header: "**Preview (first 5 rows)**"
  scripts:
    title: Scripts generated in this session
    empty: No scripts generated yet. Start a conversation in the Chat tab.
    version: "Script #{id}"
    execution_time: Execution time
    code_expander: 📄 View code
    download_btn: ⬇️ Download train.py
    upload_btn: 🚀 Upload this model to API
    upload_form_name: Model name
    upload_form_version: Version
    upload_form_desc: Description (optional)
    upload_form_algo: Algorithm
    upload_spinner: Uploading…
    upload_success: "Model {name} v{version} uploaded successfully."
    upload_error: "Upload error: {error}"
    no_model_file: This script did not produce a model file.
  dates:
    train_start: Training start date
    train_end: Training end date
nav_pages:
  home: Home
  users: Users
  models: Models
  predictions: Predictions
  stats: Statistics
  ab_testing: A/B Testing
  supervision: Supervision
  retrain: Retraining
  golden_tests: Golden Tests
  help: Help
  code_example: Code Example
  services: Services
  autotrain: AutoTrain
  login: Login
  access_request: Access Request
